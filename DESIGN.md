# TODO CLI 設計書

## 1. 概要

個人が単一マシン上で使う、軽量なTODO管理CLIツール。ターミナルからTODOの追加・一覧表示・完了・削除を行える最小限の機能を提供する。加えて、期限・優先度・タグによる管理と、ブラウザから同じデータを閲覧・操作できる画面を提供する。

### スコープ
- 単一ユーザー・単一マシンでの利用を前提とする。
- ネットワーク同期、複数デバイス間共有は行わない。
- サブタスク・繰り返しタスクなどの高度な機能は持たない(期限・優先度・タグは対応する。7章参照)。

## 2. コマンド一覧と使用例

| コマンド | 説明 |
|---|---|
| `todo add <text>` | 新規TODOを追加する |
| `todo list` (エイリアス `todo ls`) | 未完了のTODOを一覧表示する(デフォルト) |
| `todo list --all` | 完了済みも含め全件表示する |
| `todo done <id>` | 指定IDのTODOを完了にする |
| `todo rm <id>` | 指定IDのTODOを削除する |
| `todo`(引数なし) | `todo list` と同じ結果を表示する |

初回実行時、ストレージファイルが存在しなければ自動生成する(明示的な `init` コマンドは用意しない)。

上記の既存コマンドの挙動・出力・文言は変更しない(完全後方互換)。以下は今回追加する新オプション・新規サブコマンドであり、いずれも指定しなければ既存の出力と完全に一致する。

| コマンド | 説明 |
|---|---|
| `todo add <text> [--due YYYY-MM-DD] [--priority high\|medium\|low] [--tag TAG]...` | 期限・優先度・タグを指定して追加する |
| `todo list [--sort due\|priority] [--search WORD] [--status all\|pending\|done] [--tag TAG] [--due-before DATE] [--due-after DATE]` | 並び替え・検索・絞り込みを行う |
| `todo edit <id> [<text>] [--due YYYY-MM-DD] [--priority high\|medium\|low] [--tag TAG]...` | 指定IDの内容・期限・優先度・タグを編集する(指定した項目のみ更新) |
| `todo undone <id>` | 完了済みのTODOを未完了に戻す |
| `todo serve [--port 8765]` | ブラウザから閲覧・操作できるローカルサーバを起動する(8章) |

### 使用例(既存、変更なし)

```
$ todo add "牛乳を買う"
追加しました: #1 牛乳を買う

$ todo add "資料を作る"
追加しました: #2 資料を作る

$ todo list
#1 [ ] 牛乳を買う
#2 [ ] 資料を作る

$ todo done 1
完了にしました: #1 牛乳を買う

$ todo list
#2 [ ] 資料を作る

$ todo list --all
#1 [x] 牛乳を買う (完了: 09/10)
#2 [ ] 資料を作る

$ todo rm 2
削除しました: #2 資料を作る

$ todo
(TODOが0件の場合)
TODOはありません
```

### 使用例(新機能)

```
$ todo add "資料を作る" --due 2026-09-20 --priority high --tag 仕事 --tag 至急
追加しました: #3 資料を作る (期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)

$ todo list
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)

$ todo list --all
#1 [x] 牛乳を買う (完了: 09/10)
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)

$ todo edit 3 --priority medium
編集しました: #3 資料を作る

$ todo undone 1
未完了に戻しました: #1 牛乳を買う

$ todo list --sort due
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: medium, タグ: 仕事, 至急)
#1 [ ] 牛乳を買う

$ todo list --search 資料
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: medium, タグ: 仕事, 至急)

$ todo list --tag 仕事
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: medium, タグ: 仕事, 至急)

$ todo edit 99 内容
エラー: ID 99 は存在しません

$ todo add "" 
エラー: TODOの内容を入力してください

$ todo add "期限テスト" --due 2026-13-01
エラー: 期限の形式が不正です(YYYY-MM-DD形式で指定してください)

$ todo add "優先度テスト" --priority urgent
エラー: 優先度はhigh/medium/lowのいずれかで指定してください
```

## 3. データモデル

TODOアイテムは以下の構造を持つ。`due`・`priority`・`tags` は今回追加したフィールドで、いずれもoptional(省略可・デフォルト値あり)とし、既存の `todo.json`(これらのキーを持たない)をそのまま読み込める。

```json
{
  "id": 1,
  "text": "牛乳を買う",
  "done": false,
  "created_at": "2026-09-12T10:00:00+09:00",
  "completed_at": null,
  "due": null,
  "priority": null,
  "tags": []
}
```

- `id`: 1始まりの整数。削除してもIDは再利用しない(単調増加のカウンタを別途保持)。
- `text`: TODOの内容。空文字・空白のみは許可しない。改行はそのまま保存する(表示が崩れる可能性は既知の制約とする、5.5節参照)。
- `done`: 完了フラグ。
- `created_at` / `completed_at`: ISO 8601形式のタイムスタンプ。未完了時 `completed_at` は `null`。
- `due`: `"YYYY-MM-DD"` 形式の文字列、または `null`(未設定)。旧データにはキー自体が存在しないため、読み込み時は欠損を `null` として扱う。
- `priority`: `"high"` / `"medium"` / `"low"` のいずれか、または `null`(未設定)。
- `tags`: 文字列の配列。旧データにはキー自体が存在しないため、読み込み時は欠損を `[]` として扱う。重複タグは追加時に無視する(去重)。

### 互換性・ラウンドトリップ安全性の方針
- 読み込み時は `item.get("due")` のようにデフォルト補完するだけでなく、**item自体を辞書としてそのまま保持し、既知フィールドだけを抜き出して再構築しない**。これにより、将来さらにフィールドが増えた場合や、本ツールが未知のキーを持つデータを読み込んだ場合でも、書き戻し時にキーが無言で消えることを防ぐ(ラウンドトリップ安全性)。
- 新フィールドを一切使わない既存ワークフロー(`add`/`list`/`done`/`rm` のみ利用)では、保存されるJSONの構造・出力は今回の変更前と完全に同一である。

## 4. ストレージ形式と保存場所

- 保存場所: `~/.todo/todo.json`
- 形式: 単一のJSONオブジェクト。

```json
{
  "next_id": 3,
  "items": [ ... ]
}
```

### 採用理由
- 個人用途で件数が少ない前提のため、SQLiteは過剰。
- プレーンテキストでは構造化データ(done、タイムスタンプ)の扱いが煩雑。
- JSONであれば人間がエディタで直接見て理解でき、`jq` 等の外部ツールとも連携しやすい。
- データ量が小さい前提のため、差分書き込みなどの最適化は行わず、毎回全件読み込み・全件書き直しのシンプルな実装で十分とする。

### 書き込みの安全性
- 書き込みは一時ファイル(`~/.todo/todo.json.tmp`)に全件を書き出してから `rename` でアトミックに置き換える。部分書き込みによるJSON破損を防ぐ。
- 同時実行時の後勝ち上書きを防ぐため、簡易ファイルロック(ロックファイル `~/.todo/todo.lock` にPIDと取得時刻を記録する方式)を用いる。
  - ロック取得に失敗した場合は「エラー: 他のtodoプロセスが実行中です。しばらくして再試行してください」を出力し、非ゼロ終了する。
  - stale lock対策: ロックファイルに記録されたPIDが現在存在しない、または取得時刻から一定時間(例: 10秒)以上経過している場合は、そのロックを破棄して取得を続行する。プロセスクラッシュ等でロックが残り続けることを防ぐ。
- `next_id` と `items` 内の実データの整合性: `add` 実行時、新しいIDは `max(next_id, items中の最大id + 1)` を採用する。ユーザーが `jq` 等でファイルを直接編集し `next_id` と実データがずれた場合でも、ID衝突を防ぐ。
- JSONパースに失敗した場合は「エラー: データファイルが壊れています。手動で確認してください: ~/.todo/todo.json」を出力して即終了する。自動修復や自動上書きは行わない(個人データの喪失を防ぐため)。

### CLIとブラウザ画面での共有(今回追加)
- `todo serve`(8章)は、CLIと**同じ** `~/.todo/todo.json` と**同じ**ロックファイル(`~/.todo/todo.lock`)・同じアトミック書き込み(tmp+rename)・同じロック獲得ロジックを利用する。CLIとブラウザ画面を同時に使っても、単一のロック機構でデータ破損を防ぐ。
- **プロセス内直列化**: `todo serve` は複数リクエストを並行処理できる `ThreadingHTTPServer` を用いる想定のため、ファイルロック(PID単位)だけでは同一プロセス内の複数スレッドによる同時書き込みを検知できない。そのため、サーバプロセス内に `threading.Lock` を1つ持ち、ファイルロック獲得より外側でこの `threading.Lock` によりリクエストを直列化してからファイルロックを獲得する。
- **ロック保持区間の最小化**: ロック(ファイルロック・`threading.Lock` とも)を保持する区間は「JSON読み込み→更新→tmp書き込み→rename→解放」のみとし、リクエストのパースやバリデーションなど他の処理はロック外で行う。保持時間を最小化することで、stale lock判定(10秒)との誤検知リスクを下げる。
- **サーバ側のロック取得失敗時の挙動**: CLIのように即エラー終了はせず、短い間隔(例: 100ms間隔で数回)のリトライを行い、それでも取得できない場合は HTTP 503 と `{"error": "他のtodoプロセスが実行中です。しばらくして再試行してください"}` を返す(文言はCLIと共通)。

## 5. UX上の設計判断とその理由

1. **操作結果は必ず1行で返す**: `add`/`done`/`rm`/`edit`/`undone` いずれも、実行後に何をしたかを1行で明示する(例: 「削除しました: #5 資料を作る」「編集しました: #3 資料を作る」「未完了に戻しました: #1 牛乳を買う」)。学習コストを下げ、無言実行による不安を避けるため、全コマンド共通ルールとする。
2. **空状態の明示**: `todo list` が0件の場合、何も表示しないと不具合と誤解されるため「TODOはありません」を表示する。
3. **エラーメッセージの統一フォーマット**: 存在しないIDを指定した場合、`done`/`rm`/`edit`/`undone` すべてで「エラー: ID <n> は存在しません」という同一フォーマットを用いる。`add`/`edit` の内容が空の場合は「エラー: TODOの内容を入力してください」で統一する(`edit` で全項目省略時は「エラー: 変更する項目を指定してください」)。
4. **終了コード**: 成功時は `0`、エラー時は `1` を返す。スクリプトからの呼び出しを考慮し明記する。
5. **`--all` 表示での完了情報**: 完了済みは `[x]`、未完了は `[ ]` で表示し、`--all` 指定時のみ完了日時を付記する(例: `(完了: 09/10)`)。通常の `list` では完了日時を出さず一覧性を優先する。
6. **引数なし実行時はlist相当を表示**: `todo` とだけ打った場合、エラーやヘルプではなく `todo list` と同じ結果を表示する。`git` 単体の挙動と同様、発見可能性(discoverability)を優先する判断。
7. **`add` の空文字ガード**: `todo add` に引数がない、または空文字・空白のみの場合、「エラー: TODOの内容を入力してください」を出力し非ゼロ終了する。
8. **期限・優先度・タグの付記フォーマット**: 値がある項目にのみ「半角括弧+`項目名: 値`のカンマ区切り」で1つの括弧にまとめて付記する(例: `(期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)`)。`--all` 表示時の完了日時も同じ括弧内に統合する(例: `(完了: 09/10, 期限: 2026-09-20, 優先度: high)`)。括弧の種類を増やさず、既存の「(完了: 09/10)」という書式との一貫性を優先する。値を何も指定しなかった項目は括弧ごと省略されるため、既存データ・既存の使い方では表示が一切変わらない(後方互換)。
9. **期限切れの表現**: 色や記号ではなく、既存と同様にテキスト情報で表現する。期限が過去日の場合、括弧内に「期限切れ」という文字列を追記する(例: `(期限: 2026-09-01, 期限切れ)`)。
10. **`edit` の対象範囲**: `todo edit <id> [<text>] [--due] [--priority] [--tag]` とし、指定した項目のみを更新する。テキストのみの変更(`todo edit <id> <text>`)にも、オプションのみの変更(`todo edit <id> --priority low`)にも対応する。すべて省略された場合はエラーとする。
11. **`--status all` と `--all` の関係**: `--status all` は既存の `--all` と同じ意味を持つショートハンドとして扱う(実装上は同じ内部フラグにマップする)。既存の `--all` の挙動・出力は変更しない。
12. **タグ絞り込みの意味**: `--tag` を複数指定した場合はOR条件(いずれかのタグに一致)とする。個人用途の絞り込みでは条件を狭めすぎない方が使いやすいという判断による。
13. **sortの安定性とnullの扱い**: `--sort due` / `--sort priority` はPythonの安定ソートを用い、値が同じ項目同士は現在のid昇順を維持する。期限未設定(`due: null`)・優先度未設定(`priority: null`)の項目は、並び替え結果の末尾に配置する。
14. **バリデーションエラーメッセージ**: `--due` の形式不正時は「エラー: 期限の形式が不正です(YYYY-MM-DD形式で指定してください)」、`--priority` の値不正時は「エラー: 優先度はhigh/medium/lowのいずれかで指定してください」とし、既存のエラーメッセージ(「エラー: ...」)と同系統のフォーマットに揃える。`--priority` の値は大文字小文字を区別せず小文字に正規化して受理する。`--tag` は前後の空白をトリムし、空文字のタグは無視する。

## 6. 議論で出た反論とその対応

設計担当(design-agent)の初期案に対し、ux-agent(UX観点)とcritic-agent(反論役)からレビューを受け、以下のように対応した。

### 基本CLI(add/list/done/rm)に関する議論

| 指摘元 | 指摘内容 | 対応 |
|---|---|---|
| ux-agent | `rm` の成功メッセージが例に無く非対称 | 全コマンドで結果を1行返す方式に統一 |
| ux-agent | 空リスト時に何も表示しないと誤解される | 「TODOはありません」を表示 |
| ux-agent | `done` のエラーメッセージが未定義 | `rm` と同一フォーマットに統一 |
| ux-agent | 終了コードが未定義 | 成功0・エラー1と明記 |
| ux-agent | `--all` の完了マーク・完了日時表示が未定義 | `[x]` 表示と完了日時付記を採用 |
| ux-agent | 引数なし実行時の挙動が未定義 | `list` 相当を表示する方針を採用 |
| ux-agent | `add` の空文字時の挙動が未定義 | 明示的なエラーメッセージを追加 |
| critic-agent | 同時書き込み時の競合・JSON破損対策が無い(重大) | アトミック書き込み(tmp+rename)とファイルロックを採用 |
| critic-agent | JSON破損時の挙動が未定義 | エラー表示・非上書きで終了する方針を明記 |
| critic-agent | `next_id` と実データの不整合(手動編集時) | `max(next_id, 最大id+1)` を採用してID衝突を防止 |
| critic-agent | `text` のバリデーション未定義 | 空文字・空白のみは拒否、改行は許可(崩れる可能性は既知の制約) |
| critic-agent | ロックファイル方式のstale lock対策が必要 | PIDと取得時刻を記録し、プロセス不在または一定時間経過で破棄する方式を採用 |
| critic-agent | `done`/`rm` の非対称性(取り消し不可・確認なし) | 当時は意図的なスコープ判断として明記していたが、今回 `undone` を追加(下表参照)。`rm` の確認プロンプト・ゴミ箱機能は引き続きスコープ外(7章) |
| critic-agent | 出力文言をdesign-agentが決めるのは役割分担上望ましくない | 最終的な文言はux-agentとの合意を経て確定 |

### 期限・優先度・edit/undone・検索・タグ・ブラウザ画面(今回追加分)に関する議論

| 指摘元 | 指摘内容 | 対応 |
|---|---|---|
| ux-agent | `edit`/`undone` の完了メッセージ・エラーフォーマットが未定義 | 「編集しました: ...」「未完了に戻しました: ...」を新設し、エラーは既存フォーマットに統一 |
| ux-agent | 期限/優先度/タグの付記方法が未定義 | 「(項目名: 値, ...)」形式で1つの括弧にまとめる方式を採用(5.8節) |
| ux-agent | `--all` の完了日時と新項目の表示が分散すると読みにくい | 完了日時も同じ括弧内に統合(5.8節) |
| ux-agent | 期限切れの視覚表現が未定義 | 括弧内に「期限切れ」という文字列を追記(色・記号は使わない、5.9節) |
| ux-agent | `edit` がテキストのみだと期限等の変更に削除→再addが必要で不便 | `edit` を `--due`/`--priority`/`--tag` にも対応させ、指定項目のみ更新する方式に拡張(5.10節) |
| ux-agent | `--tag` 複数指定時のAND/OR意味が未定義 | OR(いずれか一致)を採用(5.12節) |
| ux-agent | ブラウザ画面の構成が未定義 | フィルタバー+追加フォーム+一覧テーブルの1画面構成を採用(8.1節) |
| ux-agent | ブラウザ画面のエラー表示を`alert()`にすると文言がCLIと分岐する | 画面上部の帯状エラー表示欄に、APIが返すCLIと共通の「エラー: ...」文言をそのまま表示する方式を採用(8.2節) |
| critic-agent | 新→旧バージョンでの互換性(未知キーが書き戻し時に消える恐れ) | 既知フィールドだけを抜き出して再構築せず、item全体をそのまま保持して書き戻す「ラウンドトリップ安全性」の方針を明記(3章) |
| critic-agent | `ThreadingHTTPServer` はファイルロック(PID単位)だけでは同一プロセス内の複数スレッドの競合を検知できない(重大) | サーバプロセス内に `threading.Lock` を追加し、ファイルロック獲得の外側でリクエストを直列化する方式を採用(4章) |
| critic-agent | ロック保持中の遅延によりstale lock(10秒)と誤判定されるリスク | ロック保持区間を「読み込み→更新→書き込み→解放」のみに最小化する方針を明記(4章) |
| critic-agent | サーバ側でロック取得に失敗した場合の挙動が未定義 | 短いリトライ後、HTTP 503とCLI共通のエラー文言を返す方式を採用(4章) |
| critic-agent | CORSヘッダを付けるとHostヘッダ検証・CSRFトークンが無意味になる | CORSヘッダは一切付与しない方針を明記(8.3節) |
| critic-agent | CSRFトークンをクエリパラメータで送るとログ・履歴に残る | カスタムヘッダ `X-Todo-Token` で送る方式に変更(8.3節) |
| critic-agent | `--due`/`--priority`/`--tag` のバリデーションが未定義 | 実在日付チェック・許容値チェック・トリムと去重を明記(5.14節) |
| critic-agent | sortの安定性・null値の順序が未定義 | 安定ソート+id昇順維持、null値は末尾に配置と明記(5.13節) |
| critic-agent | ブラウザ画面(`todo serve`)は旧7章「GUI・TUIなどCLI以外のインターフェースをやらない」と矛盾する | 依頼スコープに明示的に含まれるため機能として採用し、7章の記述を本機能に合わせて改訂(7章参照) |

### 却下(採用しなかった)提案とその理由

- **ブラウザ画面をGET専用(閲覧のみ)に縮小する案(critic-agent発案)**: 状態変更(POST/PATCH/DELETE)をすべてCLI経由に限定し、CSRF対策・スレッド安全性の懸念自体を無くす案。→ CSRFトークン(カスタムヘッダ方式)と `threading.Lock` によるプロセス内直列化により、リスクは個別に対処済みと判断し、依頼スコープにある「ブラウザからの操作」機能をそのまま維持することにした。
- **ブラウザ画面を「閲覧・検索・done/undoneトグルのみ」に限定し、add/edit(新規作成・詳細編集)はCLI優先とする段階案(ux-agent発案)**: 依頼スコープを保ちつつリスクを抑える折衷案として提案されたが、上記と同じ理由(個別の安全策で対応済み)により、add/editを含むフルスコープのAPIを採用することにした。
- なし(基本CLI部分)。基本CLIに関して挙がった指摘はいずれも個人用データの保護(アトミック性・破損対応)や一貫性向上に直結するため、すべて採用した。

## 7. スコープ外(やらないこと)

- ネットワーク同期・複数デバイス間の共有
- サブタスク、繰り返しタスクなどの高度な機能(期限・優先度・タグは対応済み、2章・3章参照)
- `rm` の確認プロンプト・ゴミ箱機能
- SQLiteなど本格的なデータベースの利用
- 大量データ向けの差分書き込み・インデックス最適化
- 自動バックアップ・自動復旧機能(破損時は手動確認を促すのみ)
- 本設計で定めたブラウザ画面(8章)を超えるインターフェース: ネイティブGUIアプリ、TUI、モバイルアプリなど

## 8. ブラウザ画面

`todo serve` は、CLIと同じ `~/.todo/todo.json` を、ブラウザから閲覧・検索・絞り込み・追加・編集・完了操作・削除できるようにするための、ローカル専用の軽量Webサーバ機能である。Python標準ライブラリの `http.server`(`ThreadingHTTPServer` / `BaseHTTPRequestHandler`)のみで実装し、追加パッケージは用いない。

### 8.1 画面構成

1画面構成とする。

- **フィルタバー**: 検索ボックス、状態セレクト(未完了/完了/すべて)、タグ絞り込み、並び替えセレクト(期限順/優先度順)
- **追加フォーム**: テキスト入力、期限、優先度、タグ、送信ボタン(ラベル「追加」)
- **一覧テーブル**: チェックボックス(完了/未完了トグル。`done`/`undone` を1つのUIで両対応)、ID、テキスト、期限、優先度、タグ、編集ボタン、削除ボタン
- **エラー表示欄**: 画面上部に帯状の常設領域を設け、`alert()` 等のブラウザ標準ダイアログは使わない。APIがCLIと共通の「エラー: ...」文言をJSONで返し、フロントエンドはその文字列をそのまま表示するのみとする(文言をCLIとブラウザで二重管理しない)。

### 8.2 API仕様

| メソッド/パス | 説明 | 対応するCLI相当 |
|---|---|---|
| `GET /api/todos` | 一覧取得。クエリパラメータで `sort`/`search`/`status`/`tag`/`due_before`/`due_after` を指定可能 | `todo list` |
| `POST /api/todos` | 新規追加。ボディに `text`/`due`/`priority`/`tags` | `todo add` |
| `PATCH /api/todos/{id}` | 編集・完了・未完了。ボディで更新したいフィールド(`text`/`due`/`priority`/`tags`/`done`)のみ指定 | `todo edit` / `todo done` / `todo undone` |
| `DELETE /api/todos/{id}` | 削除 | `todo rm` |

- 状態変更系(`POST`/`PATCH`/`DELETE`)はいずれもロック(4章)を経由し、CLIから見たときと同一のバリデーション・エラーメッセージ・ID採番規則を適用する。
- エラー応答は `{"error": "<CLIと共通の文言>"}` の形式で返す。存在しないIDに対する操作は `404`、バリデーションエラーは `400`、ロック取得失敗は `503` を返す。

### 8.3 ローカルサーバの安全性

- **バインドアドレス**: `127.0.0.1` にのみバインドする(`0.0.0.0` 等の外部公開は行わない)。
- **DNS rebinding対策**: すべてのリクエストで `Host` ヘッダを検証し、`127.0.0.1:<port>` または `localhost:<port>` 以外の場合は `403` を返す。
- **CORS**: `Access-Control-Allow-Origin` 等のCORS関連ヘッダは一切付与しない。付与するとHostヘッダ検証・CSRFトークンの意味がなくなるため。
- **CSRF対策**: サーバ起動時にランダムなトークンを生成し、状態変更系リクエスト(`POST`/`PATCH`/`DELETE`)には、カスタムヘッダ `X-Todo-Token` にこのトークンを付与することを必須とする。トークンはサーバが配信する同一オリジンのHTMLにのみ埋め込み、URLのクエリパラメータには含めない(ログ・Referrer・ブラウザ履歴への残留を避けるため)。カスタムヘッダを要求することで、CORS未許可の他オリジンからのブラウザ経由の状態変更は(プリフライトに阻まれ)構造的に不可能になる。
- **同時実行**: 4章の「CLIとブラウザ画面での共有」を参照。`threading.Lock` によるプロセス内直列化と、ファイルロックによるプロセス間排他を組み合わせる。

## 9. テスト計画

- **単体テスト**: ストレージ層(読み込み・書き込み・アトミック性・ロック取得/解放・stale lock判定)、バリデーション(`text`/`due`/`priority`/`tags`)、ID採番ロジック(`max(next_id, 最大id+1)`)を対象とする。
- **CLI結合テスト**: 既存コマンド(`add`/`list`/`ls`/`done`/`rm`)の出力が変更前と完全一致することを回帰テストで担保する。加えて、新規コマンド(`edit`/`undone`)・新規オプション(`--due`/`--priority`/`--tag`/`--sort`/`--search`/`--status`/`--due-before`/`--due-after`)の正常系・異常系(不正な日付・不正な優先度・存在しないID・全項目省略の`edit`など)を確認する。
- **互換性テスト**: 新フィールド(`due`/`priority`/`tags`)を持たない既存の `todo.json` を読み込んでも、追加操作なしでは元のファイル内容(未知キーを含む)が変化しないことを確認する(ラウンドトリップ安全性)。
- **サーバ結合テスト**: `todo serve` を起動し、API経由の `GET`/`POST`/`PATCH`/`DELETE` がCLIと同一のバリデーション・エラーメッセージ・ID採番を用いることを確認する。加えて、`Host` ヘッダ不正時の `403`、`X-Todo-Token` 欠落・不一致時の拒否、CORSヘッダが付与されないことを確認する。
- **同時実行テスト**: CLIプロセスと `todo serve` を同時に動かし、あるいは `todo serve` に対して並行リクエストを送り、`todo.json` が破損しないこと・データが失われないことを確認する(可能であればマルチスレッドからの同時書き込みをシミュレートする結合テストを用意する)。

## 10. 実装の段取り(次フェーズ、未着手)

本設計書は設計のみであり、実装はまだ行わない。実装フェーズでは以下の分担・依存関係を想定する。

| タスク | 内容 | 担当(想定) | 依存 |
|---|---|---|---|
| 実装-1 | データモデル・ストレージ層(3章・4章): 読み込み/書き込み、ラウンドトリップ安全性、ロック(ファイルロック+`threading.Lock`) | 実装担当A | なし |
| 実装-2 | CLIコマンド拡張(2章・5章): `add`/`list`拡張、`edit`/`undone`新設、バリデーション | 実装担当A or B | 実装-1 |
| 実装-3 | ブラウザ画面: サーバ本体・API(8.2章)、安全性対策(8.3章) | 実装担当B | 実装-1 |
| 実装-4 | ブラウザ画面: フロントエンド(8.1章、HTML/CSS/JS) | 実装担当B or C | 実装-3 |
| 実装-5 | テスト(9章): 単体・結合・互換性・同時実行テスト一式 | 実装担当全員(担当タスクに対応するテストは各自) | 実装-1〜4 |

各実装タスクは、本設計書(DESIGN.md)を唯一の仕様として着手し、設計と実装が乖離した場合は実装担当からdesign-agent(またはこの設計書の後継)にフィードバックし、設計書側を更新することを原則とする。
