# TODO CLI 設計書

## 1. 概要

個人が単一マシン上で使う、軽量なTODO管理CLIツール。ターミナルからTODOの追加・一覧表示・完了・削除を行える最小限の機能を提供する。加えて、期限・優先度・タグによる管理と、ブラウザから同じデータを閲覧・操作できる画面を提供する。

### スコープ
- 単一ユーザー・単一マシンでの利用を前提とする。
- ネットワーク同期、複数デバイス間共有は行わない。
- サブタスク・繰り返しタスクなどの高度な機能は持たない(期限・優先度・タグは対応する。7章参照)。

## 2. コマンド一覧と使用例

| コマンド | 説明 |
|---|---|
| `todo add <text>` | 新規TODOを追加する |
| `todo list` (エイリアス `todo ls`) | 未完了のTODOを一覧表示する(デフォルト) |
| `todo list --all` | 完了済みも含め全件表示する |
| `todo done <id>` | 指定IDのTODOを完了にする |
| `todo rm <id>` | 指定IDのTODOを削除する |
| `todo`(引数なし) | `todo list` と同じ結果を表示する |

初回実行時、ストレージファイルが存在しなければ自動生成する(明示的な `init` コマンドは用意しない)。

上記の既存コマンドの挙動・出力・文言は変更しない(完全後方互換)。以下は今回追加する新オプション・新規サブコマンドであり、いずれも指定しなければ既存の出力と完全に一致する。

| コマンド | 説明 |
|---|---|
| `todo add <text> [--due YYYY-MM-DD] [--priority high\|medium\|low] [--tag TAG]...` | 期限・優先度・タグを指定して追加する |
| `todo list [--sort due\|priority] [--search WORD] [--status all\|pending\|done] [--tag TAG] [--due-before DATE] [--due-after DATE]` | 並び替え・検索・絞り込みを行う |
| `todo edit <id> [<text>] [--due YYYY-MM-DD] [--clear-due] [--priority high\|medium\|low] [--clear-priority] [--tag TAG]... [--clear-tags]` | 指定IDの内容・期限・優先度・タグを編集する(指定した項目のみ更新) |
| `todo undone <id>` | 完了済みのTODOを未完了に戻す |
| `todo serve [--port 8765]` | ブラウザから閲覧・操作できるローカルサーバを起動する(8章) |

### `--search` の仕様(検索対象・大文字小文字)

- 検索対象は **`text`(本文)のみ**とする。`tags` は検索対象に含めない(タグでの絞り込みは別途 `--tag` オプションで行う。2つの機能の役割を分けるため)。
- 大文字小文字・全角半角は区別しない部分一致とする(例: `--search abc` は本文中の `ABC` にも一致する)。個人用途の検索で厳密な一致を要求すると使い勝手が落ちるための判断。
- 5.15節に上記仕様と根拠を記載する。

### `edit` での期限・優先度・タグの変更/解除/一部削除の仕様

- `--due`/`--priority`/`--tag` を指定した場合、その項目は指定した値で**上書き**される。`--tag` を複数指定した場合、既存のタグ配列全体を「指定した `--tag` の集合」で**置き換える**(追記ではない)。特定のタグ1つだけを外したい場合は、残したいタグをすべて `--tag` で指定し直す(例: 現在のタグが `仕事,至急` で `至急` だけを外したい場合は `todo edit 3 --tag 仕事` とする)。
- 一度設定した `due`/`priority`/`tags` を未設定(`null`/`[]`)に戻すには、専用の `--clear-due`/`--clear-priority`/`--clear-tags` フラグを使う。値指定オプション(`--due` 等)と対応する `--clear-*` を同時指定した場合はエラーとする(「エラー: --due と --clear-due は同時に指定できません」、`--priority`/`--tag` も同様の文言)。
- 5.10節にこの仕様を反映する。

### 使用例(既存、変更なし)

```
$ todo add "牛乳を買う"
追加しました: #1 牛乳を買う

$ todo add "資料を作る"
追加しました: #2 資料を作る

$ todo list
#1 [ ] 牛乳を買う
#2 [ ] 資料を作る

$ todo done 1
完了にしました: #1 牛乳を買う

$ todo list
#2 [ ] 資料を作る

$ todo list --all
#1 [x] 牛乳を買う (完了: 09/10)
#2 [ ] 資料を作る

$ todo rm 2
削除しました: #2 資料を作る

$ todo
(TODOが0件の場合)
TODOはありません
```

### 使用例(新機能)

```
$ todo add "資料を作る" --due 2026-09-20 --priority high --tag 仕事 --tag 至急
追加しました: #3 資料を作る (期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)

$ todo list
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)

$ todo list --all
#1 [x] 牛乳を買う (完了: 09/10)
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)

$ todo edit 3 --priority medium
編集しました: #3 資料を作る

$ todo undone 1
未完了に戻しました: #1 牛乳を買う

$ todo list --sort due
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: medium, タグ: 仕事, 至急)
#1 [ ] 牛乳を買う

$ todo list --search 資料
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: medium, タグ: 仕事, 至急)

$ todo list --search SIRYOU
(本文に一致しないため0件。--search は本文のみが対象でローマ字変換は行わない)
TODOはありません

$ todo list --tag 仕事
#3 [ ] 資料を作る (期限: 2026-09-20, 優先度: medium, タグ: 仕事, 至急)

$ todo edit 3 --tag 仕事
編集しました: #3 資料を作る

$ todo list --tag 至急
(--tagを付け直したことで「至急」タグは外れたため0件)
TODOはありません

$ todo edit 3 --clear-due --clear-priority --clear-tags
編集しました: #3 資料を作る

$ todo list --all
#1 [ ] 牛乳を買う
#3 [ ] 資料を作る

$ todo edit 99 内容
エラー: ID 99 は存在しません

$ todo add "" 
エラー: TODOの内容を入力してください

$ todo add "期限テスト" --due 2026-13-01
エラー: 期限の形式が不正です(YYYY-MM-DD形式で指定してください)

$ todo add "優先度テスト" --priority urgent
エラー: 優先度はhigh/medium/lowのいずれかで指定してください

$ todo edit 3 --due 2026-09-25 --clear-due
エラー: --due と --clear-due は同時に指定できません
```

## 3. データモデル

TODOアイテムは以下の構造を持つ。`due`・`priority`・`tags` は今回追加したフィールドで、いずれもoptional(省略可・デフォルト値あり)とし、既存の `todo.json`(これらのキーを持たない)をそのまま読み込める。

```json
{
  "id": 1,
  "text": "牛乳を買う",
  "done": false,
  "created_at": "2026-09-12T10:00:00+09:00",
  "completed_at": null,
  "due": null,
  "priority": null,
  "tags": []
}
```

- `id`: 1始まりの整数。削除してもIDは再利用しない(単調増加のカウンタを別途保持)。
- `text`: TODOの内容。空文字・空白のみは許可しない。改行はそのまま保存する(表示が崩れる可能性は既知の制約とする、5.5節参照)。
- `done`: 完了フラグ。
- `created_at` / `completed_at`: ISO 8601形式のタイムスタンプ。未完了時 `completed_at` は `null`。
- `due`: `"YYYY-MM-DD"` 形式の文字列、または `null`(未設定)。旧データにはキー自体が存在しないため、読み込み時は欠損を `null` として扱う。
- `priority`: `"high"` / `"medium"` / `"low"` のいずれか、または `null`(未設定)。
- `tags`: 文字列の配列。旧データにはキー自体が存在しないため、読み込み時は欠損を `[]` として扱う。重複タグは追加時に無視する(去重)。

### テスト可能性(時刻依存の排除)
- `created_at`/`completed_at` の生成、および5.9節の「期限切れ」判定に使う現在日時は、ストレージ層内の関数(現在時刻用 `now()`、現在日付用 `today()`)を経由してのみ取得する。呼び出し側(`add`/`done`/`edit`/`--due-before`等の判定ロジック)はこれらの関数を**モジュール経由で呼び出す**(例: `storage.now()`。`from storage import now` のように直接名前を束縛すると、テストからの差し替え(monkeypatch)が効かなくなるため避ける)。
- テストコードはこれらの関数を任意の日時に固定した上で `add`/`done`/期限切れ判定をアサートすることで、実行時刻に依存しない決定的なテストを書ける(9章参照)。

### 互換性・ラウンドトリップ安全性の方針
- 読み込み時は `item.get("due")` のようにデフォルト補完するだけでなく、**item自体を辞書としてそのまま保持し、既知フィールドだけを抜き出して再構築しない**。これにより、将来さらにフィールドが増えた場合や、本ツールが未知のキーを持つデータを読み込んだ場合でも、書き戻し時にキーが無言で消えることを防ぐ(ラウンドトリップ安全性)。
- 新フィールドを一切使わない既存ワークフロー(`add`/`list`/`done`/`rm` のみ利用)では、保存されるJSONの構造・出力は今回の変更前と完全に同一である。

## 4. ストレージ形式と保存場所

- 保存場所: リポジトリ直下の `.todo-data/todo.json`(`todo/storage.py` の `__file__` からリポジトリルートを求めて解決するため、実行時のカレントディレクトリに左右されない)。ただし環境変数 `TODO_HOME` が設定されている場合は、`.todo-data` の代わりに `TODO_HOME` が指す(展開・絶対パス化した)ディレクトリを使用する(例: `TODO_HOME=/tmp/todo-test` なら `/tmp/todo-test/todo.json`)。本ツールは試作段階であり、ホームディレクトリ(`~/.todo`)には一切保存しない方針とする。この差し替えは、単体テスト・結合テストが実行環境の実データを書き換えてしまうのを避けるためのテスト分離用途を主目的とする(9章参照)。
- 形式: 単一のJSONオブジェクト。

```json
{
  "next_id": 3,
  "items": [ ... ]
}
```

### 採用理由
- 個人用途で件数が少ない前提のため、SQLiteは過剰。
- プレーンテキストでは構造化データ(done、タイムスタンプ)の扱いが煩雑。
- JSONであれば人間がエディタで直接見て理解でき、`jq` 等の外部ツールとも連携しやすい。
- データ量が小さい前提のため、差分書き込みなどの最適化は行わず、毎回全件読み込み・全件書き直しのシンプルな実装で十分とする。

### 書き込みの安全性
- 書き込みは一時ファイル(`.todo-data/todo.json.tmp`)に全件を書き出してから `rename` でアトミックに置き換える。部分書き込みによるJSON破損を防ぐ。
- 同時実行時の後勝ち上書きを防ぐため、簡易ファイルロック(ロックファイル `.todo-data/todo.lock` にPIDと取得時刻を記録する方式)を用いる。
  - ロック取得に失敗した場合は「エラー: 他のtodoプロセスが実行中です。しばらくして再試行してください」を出力し、非ゼロ終了する。
  - stale lock対策: ロックファイルに記録されたPIDが現在存在しない、または取得時刻から一定時間(デフォルト10秒。環境変数 `TODO_LOCK_STALE_SECONDS` で上書き可能)以上経過している場合は、そのロックを破棄して取得を続行する。プロセスクラッシュ等でロックが残り続けることを防ぐ。`TODO_LOCK_STALE_SECONDS` はテストでこの閾値を数秒未満に短縮し、10秒待たずにstale lock判定のテストを実行できるようにする目的で用意する(通常運用では設定不要)。
- `next_id` と `items` 内の実データの整合性: `add` 実行時、新しいIDは `max(next_id, items中の最大id + 1)` を採用する。ユーザーが `jq` 等でファイルを直接編集し `next_id` と実データがずれた場合でも、ID衝突を防ぐ。
- JSONパースに失敗した場合は「エラー: データファイルが壊れています。手動で確認してください: <保存先ディレクトリ>/todo.json」を出力して即終了する。自動修復や自動上書きは行わない(個人データの喪失を防ぐため)。

### ロック機構の置き場所とバリデーションの一本化(10章の依存関係整理の前提)
- ファイルロック・stale lock判定・アトミック書き込みは、CLIとサーバのどちらからも呼び出す共通ロジックのため、ストレージ層(`todo/storage.py`、10章参照)に置き、**公開関数**として提供する(`acquire_lock()` / `LockGuard`。先頭アンダースコア無しの名前とし、`cli.py`・`server.py`のどちらからもインポートして使える形にする)。
- `add`/`edit`/`done`/`undone`/`rm` の入力バリデーション(`text`の空文字チェック、`due`の日付形式・`priority`の許容値・`tags`のトリム/去重など)も同様にストレージ層の関数(`add()`/`edit()`等)の内部で行い、CLIとサーバの双方がこの関数を呼び出すだけで同一のバリデーション結果を得る。これにより、サーバ側(8章)はCLI側の実装を待たずにストレージ層の関数を直接呼び出して実装でき、10章のタスク依存関係を単純化できる(実装着手.mdの指摘への対応、6章参照)。

### CLIとブラウザ画面での共有(今回追加)
- `todo serve`(8章)は、CLIと**同じ** `.todo-data/todo.json` と**同じ**ロックファイル(`.todo-data/todo.lock`)・同じアトミック書き込み(tmp+rename)・同じロック獲得ロジックを利用する。CLIとブラウザ画面を同時に使っても、単一のロック機構でデータ破損を防ぐ。
- **プロセス内直列化**: `todo serve` は複数リクエストを並行処理できる `ThreadingHTTPServer` を用いる想定のため、ファイルロック(PID単位)だけでは同一プロセス内の複数スレッドによる同時書き込みを検知できない。そのため、サーバプロセス内に `threading.Lock` を1つ持ち、ファイルロック獲得より外側でこの `threading.Lock` によりリクエストを直列化してからファイルロックを獲得する。
- **ロック保持区間の最小化**: ロック(ファイルロック・`threading.Lock` とも)を保持する区間は「JSON読み込み→更新→tmp書き込み→rename→解放」のみとし、リクエストのパースやバリデーションなど他の処理はロック外で行う。保持時間を最小化することで、stale lock判定(10秒)との誤検知リスクを下げる。
- **サーバ側のロック取得失敗時の挙動**: CLIのように即エラー終了はせず、短い間隔(例: 100ms間隔で数回)のリトライを行い、それでも取得できない場合は HTTP 503 と `{"error": "他のtodoプロセスが実行中です。しばらくして再試行してください"}` を返す(文言はCLIと共通)。

## 5. UX上の設計判断とその理由

1. **操作結果は必ず1行で返す**: `add`/`done`/`rm`/`edit`/`undone` いずれも、実行後に何をしたかを1行で明示する(例: 「削除しました: #5 資料を作る」「編集しました: #3 資料を作る」「未完了に戻しました: #1 牛乳を買う」)。学習コストを下げ、無言実行による不安を避けるため、全コマンド共通ルールとする。
2. **空状態の明示**: `todo list` が0件の場合、何も表示しないと不具合と誤解されるため「TODOはありません」を表示する。
3. **エラーメッセージの統一フォーマット**: 存在しないIDを指定した場合、`done`/`rm`/`edit`/`undone` すべてで「エラー: ID <n> は存在しません」という同一フォーマットを用いる。`add`/`edit` の内容が空の場合は「エラー: TODOの内容を入力してください」で統一する(`edit` で全項目省略時は「エラー: 変更する項目を指定してください」)。
4. **終了コード**: 成功時は `0`、エラー時は `1` を返す。スクリプトからの呼び出しを考慮し明記する。
5. **`--all` 表示での完了情報**: 完了済みは `[x]`、未完了は `[ ]` で表示し、`--all` 指定時のみ完了日時を付記する(例: `(完了: 09/10)`)。通常の `list` では完了日時を出さず一覧性を優先する。
6. **引数なし実行時はlist相当を表示**: `todo` とだけ打った場合、エラーやヘルプではなく `todo list` と同じ結果を表示する。`git` 単体の挙動と同様、発見可能性(discoverability)を優先する判断。
7. **`add` の空文字ガード**: `todo add` に引数がない、または空文字・空白のみの場合、「エラー: TODOの内容を入力してください」を出力し非ゼロ終了する。
8. **期限・優先度・タグの付記フォーマット**: 値がある項目にのみ「半角括弧+`項目名: 値`のカンマ区切り」で1つの括弧にまとめて付記する(例: `(期限: 2026-09-20, 優先度: high, タグ: 仕事, 至急)`)。`--all` 表示時の完了日時も同じ括弧内に統合する(例: `(完了: 09/10, 期限: 2026-09-20, 優先度: high)`)。括弧の種類を増やさず、既存の「(完了: 09/10)」という書式との一貫性を優先する。値を何も指定しなかった項目は括弧ごと省略されるため、既存データ・既存の使い方では表示が一切変わらない(後方互換)。
9. **期限切れの表現**: 色や記号ではなく、既存と同様にテキスト情報で表現する。期限が過去日の場合、括弧内に「期限切れ」という文字列を追記する(例: `(期限: 2026-09-01, 期限切れ)`)。
10. **`edit` の対象範囲**: `todo edit <id> [<text>] [--due] [--clear-due] [--priority] [--clear-priority] [--tag]... [--clear-tags]` とし、指定した項目のみを更新する。テキストのみの変更(`todo edit <id> <text>`)にも、オプションのみの変更(`todo edit <id> --priority low`)にも対応する。すべて省略された場合はエラーとする。`--tag` を指定した場合は既存のタグ配列全体を指定した集合で**置き換える**(追記ではない)。特定の1タグだけを外したい場合は残すタグを指定し直す。`due`/`priority`/`tags` を未設定に戻したい場合は、値指定オプションではなく `--clear-due`/`--clear-priority`/`--clear-tags` を使う(値指定オプションと対応する `--clear-*` の同時指定はエラー)。
11. **`--status all` と `--all` の関係**: `--status all` は既存の `--all` と同じ意味を持つショートハンドとして扱う(実装上は同じ内部フラグにマップする)。既存の `--all` の挙動・出力は変更しない。
12. **タグ絞り込みの意味**: `--tag` を複数指定した場合はOR条件(いずれかのタグに一致)とする。個人用途の絞り込みでは条件を狭めすぎない方が使いやすいという判断による。
13. **sortの安定性とnullの扱い**: `--sort due` / `--sort priority` はPythonの安定ソートを用い、値が同じ項目同士は現在のid昇順を維持する。期限未設定(`due: null`)・優先度未設定(`priority: null`)の項目は、並び替え結果の末尾に配置する。
14. **バリデーションエラーメッセージ**: `--due` の形式不正時は「エラー: 期限の形式が不正です(YYYY-MM-DD形式で指定してください)」、`--priority` の値不正時は「エラー: 優先度はhigh/medium/lowのいずれかで指定してください」とし、既存のエラーメッセージ(「エラー: ...」)と同系統のフォーマットに揃える。`--priority` の値は大文字小文字を区別せず小文字に正規化して受理する。`--tag` は前後の空白をトリムし、空文字のタグは無視する。
15. **`--search` の対象と一致判定**: 検索対象は `text`(本文)のみとし、`tags` は対象に含めない(タグの絞り込みは `--tag` の役割とし機能を重複させない)。大文字小文字・全角半角を区別しない部分一致とする(2章参照)。実装上は比較前に検索語・本文の双方をUnicode正規化(NFKC)した上で小文字化して比較する。9章の単体テストで、半角/全角・大文字/小文字の混在パターン(例: `--search ABC` が `ａｂｃ` を含む本文にも一致すること)を確認する。
16. **`--due-before`/`--due-after` の境界値**: いずれも指定日を**含む**(境界値を含む閉区間)。すなわち `--due-before 2026-09-20` は期限が2026-09-20以前(当日を含む)の項目を、`--due-after 2026-09-20` は期限が2026-09-20以降(当日を含む)の項目を対象とする。「今日まで/今日から」のような日常的な言い回しに合わせ、境界日を除外して驚かせないことを優先した。両方を指定した場合はAND条件(両方の範囲に一致)とする。

## 6. 議論で出た反論とその対応

設計担当(design-agent)の初期案に対し、ux-agent(UX観点)とcritic-agent(反論役)からレビューを受け、以下のように対応した。

### 基本CLI(add/list/done/rm)に関する議論

| 指摘元 | 指摘内容 | 対応 |
|---|---|---|
| ux-agent | `rm` の成功メッセージが例に無く非対称 | 全コマンドで結果を1行返す方式に統一 |
| ux-agent | 空リスト時に何も表示しないと誤解される | 「TODOはありません」を表示 |
| ux-agent | `done` のエラーメッセージが未定義 | `rm` と同一フォーマットに統一 |
| ux-agent | 終了コードが未定義 | 成功0・エラー1と明記 |
| ux-agent | `--all` の完了マーク・完了日時表示が未定義 | `[x]` 表示と完了日時付記を採用 |
| ux-agent | 引数なし実行時の挙動が未定義 | `list` 相当を表示する方針を採用 |
| ux-agent | `add` の空文字時の挙動が未定義 | 明示的なエラーメッセージを追加 |
| critic-agent | 同時書き込み時の競合・JSON破損対策が無い(重大) | アトミック書き込み(tmp+rename)とファイルロックを採用 |
| critic-agent | JSON破損時の挙動が未定義 | エラー表示・非上書きで終了する方針を明記 |
| critic-agent | `next_id` と実データの不整合(手動編集時) | `max(next_id, 最大id+1)` を採用してID衝突を防止 |
| critic-agent | `text` のバリデーション未定義 | 空文字・空白のみは拒否、改行は許可(崩れる可能性は既知の制約) |
| critic-agent | ロックファイル方式のstale lock対策が必要 | PIDと取得時刻を記録し、プロセス不在または一定時間経過で破棄する方式を採用 |
| critic-agent | `done`/`rm` の非対称性(取り消し不可・確認なし) | 当時は意図的なスコープ判断として明記していたが、今回 `undone` を追加(下表参照)。`rm` の確認プロンプト・ゴミ箱機能は引き続きスコープ外(7章) |
| critic-agent | 出力文言をdesign-agentが決めるのは役割分担上望ましくない | 最終的な文言はux-agentとの合意を経て確定 |

### 期限・優先度・edit/undone・検索・タグ・ブラウザ画面(今回追加分)に関する議論

| 指摘元 | 指摘内容 | 対応 |
|---|---|---|
| ux-agent | `edit`/`undone` の完了メッセージ・エラーフォーマットが未定義 | 「編集しました: ...」「未完了に戻しました: ...」を新設し、エラーは既存フォーマットに統一 |
| ux-agent | 期限/優先度/タグの付記方法が未定義 | 「(項目名: 値, ...)」形式で1つの括弧にまとめる方式を採用(5.8節) |
| ux-agent | `--all` の完了日時と新項目の表示が分散すると読みにくい | 完了日時も同じ括弧内に統合(5.8節) |
| ux-agent | 期限切れの視覚表現が未定義 | 括弧内に「期限切れ」という文字列を追記(色・記号は使わない、5.9節) |
| ux-agent | `edit` がテキストのみだと期限等の変更に削除→再addが必要で不便 | `edit` を `--due`/`--priority`/`--tag` にも対応させ、指定項目のみ更新する方式に拡張(5.10節) |
| ux-agent | `--tag` 複数指定時のAND/OR意味が未定義 | OR(いずれか一致)を採用(5.12節) |
| ux-agent | ブラウザ画面の構成が未定義 | フィルタバー+追加フォーム+一覧テーブルの1画面構成を採用(8.1節) |
| ux-agent | ブラウザ画面のエラー表示を`alert()`にすると文言がCLIと分岐する | 画面上部の帯状エラー表示欄に、APIが返すCLIと共通の「エラー: ...」文言をそのまま表示する方式を採用(8.2節) |
| critic-agent | 新→旧バージョンでの互換性(未知キーが書き戻し時に消える恐れ) | 既知フィールドだけを抜き出して再構築せず、item全体をそのまま保持して書き戻す「ラウンドトリップ安全性」の方針を明記(3章) |
| critic-agent | `ThreadingHTTPServer` はファイルロック(PID単位)だけでは同一プロセス内の複数スレッドの競合を検知できない(重大) | サーバプロセス内に `threading.Lock` を追加し、ファイルロック獲得の外側でリクエストを直列化する方式を採用(4章) |
| critic-agent | ロック保持中の遅延によりstale lock(10秒)と誤判定されるリスク | ロック保持区間を「読み込み→更新→書き込み→解放」のみに最小化する方針を明記(4章) |
| critic-agent | サーバ側でロック取得に失敗した場合の挙動が未定義 | 短いリトライ後、HTTP 503とCLI共通のエラー文言を返す方式を採用(4章) |
| critic-agent | CORSヘッダを付けるとHostヘッダ検証・CSRFトークンが無意味になる | CORSヘッダは一切付与しない方針を明記(8.3節) |
| critic-agent | CSRFトークンをクエリパラメータで送るとログ・履歴に残る | カスタムヘッダ `X-Todo-Token` で送る方式に変更(8.3節) |
| critic-agent | `--due`/`--priority`/`--tag` のバリデーションが未定義 | 実在日付チェック・許容値チェック・トリムと去重を明記(5.14節) |
| critic-agent | sortの安定性・null値の順序が未定義 | 安定ソート+id昇順維持、null値は末尾に配置と明記(5.13節) |
| critic-agent | ブラウザ画面(`todo serve`)は旧7章「GUI・TUIなどCLI以外のインターフェースをやらない」と矛盾する | 依頼スコープに明示的に含まれるため機能として採用し、7章の記述を本機能に合わせて改訂(7章参照) |

### 追加レビュー(review/配下の4ファイル)への対応

一度完成した本設計書に対し、依頼照合・操作の網羅・テスト可能性・実装着手の4観点で追加レビューを受けた。指摘ごとに採用/不採用と対応箇所を以下に記録する。

| 指摘元 | 指摘内容 | 対応 |
|---|---|---|
| review/依頼照合.md | 依頼の「実装の段取り: どのファイルを誰が持つか」に対し、10章の担当がチャプター単位(実装担当A/B/C)にとどまり、ファイル単位になっていない(項目7、部分的判定) | 採用。10章を全面改訂し、`todo/storage.py`等の具体的ファイル名ごとに担当1名を確定する形に書き直した(10章参照) |
| review/依頼照合.md | 他の6項目(使用例・データモデル・画面/API・ロック・サーバ安全性・テスト計画)と完了の定義3要素は「はい」判定 | 追加対応なし(既存の記述を維持) |
| review/操作の網羅.md | タグの個別削除操作が未定義(`edit --tag`の置換/追加の意味論が不明) | 採用。`edit --tag`は既存タグ配列を全置換する仕様と明記し、特定タグを外すには残すタグを指定し直す運用とした(2章・5.10節) |
| review/操作の網羅.md | due/priorityを未設定に戻す操作が未定義 | 採用。`--clear-due`/`--clear-priority`/`--clear-tags`を新設し、CLI・API(PATCHでの`null`/`[]`)双方に定義した(2章・5.10節・8.2節) |
| review/操作の網羅.md | text編集の画面側UI導線(編集ボタンが何をするか)が未定義 | 採用。編集ボタン押下でインライン編集モードに切り替わる操作を明記した(8.1節) |
| review/操作の網羅.md | text/due/priority/tagsのundo/revert機能がスコープ内かスコープ外か読み取れない | 採用。7章「スコープ外」に、`done`/`undone`以外の変更履歴・取り消し機能は対象外である旨を明記した(7章) |
| review/テスト可能性.md | `created_at`/`completed_at`の現在時刻依存を固定・注入する方法が未記載 | 採用。ストレージ層に`now()`を設け、テストからmonkeypatch可能にする方針を明記した(3章・9章) |
| review/テスト可能性.md | 期限切れ判定(5.9節)の現在日付依存を固定する方法が未記載 | 採用。同様に`today()`を設け、テストから固定可能にする方針を明記した(3章・9章) |
| review/テスト可能性.md | CSRFトークンをテストコードがどう取得するかが未記載 | 採用。トークンをHTMLの`<meta>`タグに埋め込み、テストは`GET /`を取得してパースする方法を明記した(8.3節・9章) |
| review/テスト可能性.md | 固定ポート8765のみで、テスト実行時のポート衝突回避方法が未記載 | 採用。`--port 0`によるOSへの動的割り当てと、実際のポートを標準出力に出力する仕様を追加した(8.3節・9章) |
| review/テスト可能性.md | 保存場所(`~/.todo`)をテスト用に差し替える方法が未記載(実データ破壊リスク) | 採用(リーダー指定の追加3点bと同一)。環境変数`TODO_HOME`による差し替えを4章に明記した |
| review/テスト可能性.md | stale lock判定の待ち時間(10秒)をテストで短縮する方法が未記載 | 採用。環境変数`TODO_LOCK_STALE_SECONDS`で上書き可能とする方針を追加した(4章・9章) |
| review/テスト可能性.md | PID存在確認をテストでどう固定するかが未記載 | 採用。OSのプロセス管理をモックせず、明らかに存在しない大きなPID値を使う指針を9章に明記した(コード変更は不要と判断) |
| review/実装着手.md | 実装-3(サーバ)が実装-2(CLI)のバリデーションに依存するのに、10章の依存関係に書かれていない | 採用。バリデーション・ロックを`todo/storage.py`に一本化し、実装-3は実装-2ではなく実装-1のみに依存する形に設計を変更した(4章・10章) |
| review/実装着手.md | ロック関連ロジックをどのモジュールに置き、CLI/サーバの両方からどう呼ぶかが未定義 | 採用。`todo/storage.py`に公開関数`acquire_lock()`/`LockGuard`として置き、両方から直接呼び出す方針を明記した(4章・10章) |
| review/実装着手.md | `edit --tag`が追記か置換かが未定義 | 採用。操作の網羅.mdの同一指摘と合わせて全置換と確定した(2章・5.10節) |
| review/実装着手.md | `--due-before`/`--due-after`の境界値(以上/以下)が未定義 | 採用。いずれも指定日を含む(閉区間)と確定した(5.16節) |
| review/実装着手.md | CSRFトークンの生成方法が未指定 | 採用。`secrets.token_hex(32)`を用いると明記した(8.3節) |
| review/実装着手.md | 担当表の「実装担当A or B」等の併記が確定していない | 採用。10章をファイル単位・担当1名確定の形に全面改訂した(10章) |
| review/実装着手.md | ファイル構成・ファイル単位の担当が10章に記載されていない(依頼照合.mdと同一論点) | 採用。10章に`todo/server.py`等の新規ファイルを含む具体的なファイル構成と担当を明記した(10章) |

### 改訂ドラフトに対するux-agent・critic-agentの再レビュー

上記の改訂ドラフトを、ux-agent・critic-agentが改めてレビューし、以下の指摘を受けた。

| 指摘元 | 指摘内容 | 対応 |
|---|---|---|
| ux-agent | 2章と5.15節で`--search`の仕様説明が食い違う(全角半角の扱いが5.15節に無い) | 採用。5.15節に全角半角を区別しない旨とNFKC正規化での実装方針を追記し、9章にテスト観点を追加した |
| ux-agent | 8.1節のインライン編集で、due/priorityを未設定に戻すブラウザ側のUI操作が未定義 | 採用。優先度`<select>`の「(未設定)」選択肢、期限・タグ入力欄を空にする操作が、それぞれPATCHの`null`/`[]`(8.2節)に対応する旨を8.1節に明記した |
| ux-agent | インライン編集でPATCHがバリデーションエラーになった場合の画面挙動が未定義 | 採用。エラー時は編集モードと入力値を維持し、エラー表示欄にメッセージを表示する方針を8.1節に明記した |
| ux-agent | 追加フォームで期限・優先度・タグが未入力でも送信できることが明記されていない | 採用。テキストのみが必須でありCLIと同じ挙動になる旨を8.1節に追記した |
| critic-agent | `todo serve`のディスパッチ(実装-2)は実装-3のサーバ起動関数を呼ぶ必要があり、10章の「実装-2と実装-3は互いに依存しない」という整理と矛盾する | 採用。`todo.server.run_server(host, port)`という最小限の公開関数シグネチャを8.4節で確定し、実装-2はこのシグネチャにのみ依存する(実装-3の内部実装完成を待たない)形に整理した |
| critic-agent | CSRFトークンをHTMLへ埋め込む処理(実装-3)は、`index.html`(実装-4)のプレースホルダ構造を知らないと実装できず、10章に逆方向の依存が抜けている | 採用。`<meta name="todo-csrf-token" content="__CSRF_TOKEN__">`という固定プレースホルダ文字列を8.3節で契約として確定し、実装-3・実装-4はこの文字列にのみ依存する形にした |
| critic-agent | 6章に「4者の最終合意」の一文が無いことの確認 | 当時は4者のレビューが出そろってから明記する予定として意図的に未記載だったが、その後acceptance-agentの受け入れ確認が完了したため、6章「最終合意」節に4者(design/ux/critic/acceptance)の合意を明記した |
| critic-agent | 8.4節の`run_server`に停止手段が無く、結合テスト・同時実行テストでスレッド/ソケットがリークする | 採用。`create_server`(ブロックしない、サーバオブジェクトを返す)と`run_server`(`create_server`を呼びブロックする)に分離し、テストは`create_server`を使って`shutdown()`/`server_close()`で明示的に後片付けする契約を8.4節・9章に追記した |

### 却下(採用しなかった)提案とその理由

- **ブラウザ画面をGET専用(閲覧のみ)に縮小する案(critic-agent発案)**: 状態変更(POST/PATCH/DELETE)をすべてCLI経由に限定し、CSRF対策・スレッド安全性の懸念自体を無くす案。→ CSRFトークン(カスタムヘッダ方式)と `threading.Lock` によるプロセス内直列化により、リスクは個別に対処済みと判断し、依頼スコープにある「ブラウザからの操作」機能をそのまま維持することにした。
- **ブラウザ画面を「閲覧・検索・done/undoneトグルのみ」に限定し、add/edit(新規作成・詳細編集)はCLI優先とする段階案(ux-agent発案)**: 依頼スコープを保ちつつリスクを抑える折衷案として提案されたが、上記と同じ理由(個別の安全策で対応済み)により、add/editを含むフルスコープのAPIを採用することにした。
- なし(基本CLI部分)。基本CLIに関して挙がった指摘はいずれも個人用データの保護(アトミック性・破損対応)や一貫性向上に直結するため、すべて採用した。

### 最終合意

design-agent・ux-agent・critic-agentの3者は、review/配下4ファイルの指摘とリーダーからの追加3点をすべて反映した本改訂ドラフトについて、ux-agent・critic-agentから追加の新規指摘が出ない状態(いずれも「他に指摘なし」と表明)に至ったことを確認し、最終版として合意した。続けてacceptance-agentが受け入れ確認(全指摘の行番号照合)を行い、全項目OKと判定した。以上により、design-agent・ux-agent・critic-agent・acceptance-agentの4者が本設計書の最終版に合意した(2026-09-12)。

## 7. スコープ外(やらないこと)

- ネットワーク同期・複数デバイス間の共有
- サブタスク、繰り返しタスクなどの高度な機能(期限・優先度・タグは対応済み、2章・3章参照)
- `rm` の確認プロンプト・ゴミ箱機能
- SQLiteなど本格的なデータベースの利用
- 大量データ向けの差分書き込み・インデックス最適化
- 自動バックアップ・自動復旧機能(破損時は手動確認を促すのみ)
- 本設計で定めたブラウザ画面(8章)を超えるインターフェース: ネイティブGUIアプリ、TUI、モバイルアプリなど
- `text`/`due`/`priority`/`tags` の変更履歴の保持・直前の値への取り消し(undo/revert)機能。`done`/`undone`(完了状態の取り消し)のみ両方向の操作を提供し、それ以外のフィールドは編集(`edit`)で新しい値を明示的に指定し直すことのみをサポートする(操作の網羅.mdの指摘への対応、6章参照)。

## 8. ブラウザ画面

`todo serve` は、CLIと同じ `.todo-data/todo.json` を、ブラウザから閲覧・検索・絞り込み・追加・編集・完了操作・削除できるようにするための、ローカル専用の軽量Webサーバ機能である。Python標準ライブラリの `http.server`(`ThreadingHTTPServer` / `BaseHTTPRequestHandler`)のみで実装し、追加パッケージは用いない。

### 8.1 画面構成

1画面構成とする。

- **フィルタバー**: 検索ボックス、状態セレクト(未完了/完了/すべて)、タグ絞り込み、並び替えセレクト(期限順/優先度順)
- **追加フォーム**: テキスト入力、期限、優先度、タグ、送信ボタン(ラベル「追加」)。テキスト以外(期限・優先度・タグ)はすべて未入力のまま送信でき、その場合は `todo add <text>` と同じ挙動になる(CLIと同じく必須項目はテキストのみ、2章参照)。
- **一覧テーブル**: チェックボックス(完了/未完了トグル。`done`/`undone` を1つのUIで両対応)、ID、テキスト、期限、優先度、タグ、編集ボタン、削除ボタン
- **エラー表示欄**: 画面上部に帯状の常設領域を設け、`alert()` 等のブラウザ標準ダイアログは使わない。APIがCLIと共通の「エラー: ...」文言をJSONで返し、フロントエンドはその文字列をそのまま表示するのみとする(文言をCLIとブラウザで二重管理しない)。

**編集ボタンの操作**: 編集ボタンを押すと、その行がインライン編集モードに切り替わる(テキスト・期限・優先度・タグの各セルが、モーダルを開かずその場で入力可能な状態になる)。行内に「保存」「キャンセル」ボタンが表示され、「保存」で `PATCH /api/todos/{id}` を送信して通常表示に戻り、「キャンセル」で変更を破棄して元の表示に戻す。タグは編集モード中もカンマ区切りのテキスト入力欄とし、保存時に配列へ変換して送信する(既存タグの全置換。2章・5.10節と同じ意味論)。

- **未設定に戻す操作(ブラウザ側)**: 優先度の `<select>` には「(未設定)」という選択肢を用意し、これを選ぶと `priority: null` をPATCHで送る。期限の入力欄を空にして保存すると `due: null` を送る。タグの入力欄を空にして保存すると `tags: []` を送る。いずれもCLIの `--clear-priority`/`--clear-due`/`--clear-tags`(2章・5.10節)と同じ結果になる(8.2節のnull/[]によるclear仕様と対応)。
- **保存時にAPIがエラーを返した場合**: 行は通常表示に戻さず、インライン編集モードを維持したまま入力値も保持する(入力し直しの手間を避けるため)。エラー内容は画面上部のエラー表示欄(上記)にAPIが返した「エラー: ...」文言をそのまま表示する。ユーザーは編集モードのまま値を修正して再度「保存」を押せる。「キャンセル」を押した場合のみ入力内容を破棄して通常表示に戻る。

### 8.2 API仕様

| メソッド/パス | 説明 | 対応するCLI相当 |
|---|---|---|
| `GET /api/todos` | 一覧取得。クエリパラメータで `sort`/`search`/`status`/`tag`/`due_before`/`due_after` を指定可能 | `todo list` |
| `POST /api/todos` | 新規追加。ボディに `text`/`due`/`priority`/`tags` | `todo add` |
| `PATCH /api/todos/{id}` | 編集・完了・未完了。ボディで更新したいフィールド(`text`/`due`/`priority`/`tags`/`done`)のみ指定 | `todo edit` / `todo done` / `todo undone` |
| `DELETE /api/todos/{id}` | 削除 | `todo rm` |

- 状態変更系(`POST`/`PATCH`/`DELETE`)はいずれもロック(4章)を経由し、CLIから見たときと同一のバリデーション・エラーメッセージ・ID採番規則を適用する(4章「ロック機構の置き場所とバリデーションの一本化」参照。サーバはストレージ層の関数を直接呼び出すのみで、CLI固有のコードには依存しない)。
- `PATCH` ボディで `due`/`priority`/`tags` を明示的に `null`(`tags` の場合は `[]`)として送ると、CLIの `--clear-due`/`--clear-priority`/`--clear-tags` と同じ「未設定に戻す」操作になる。フィールド自体をボディに含めなければ「変更しない」を意味する(JSONの「キー無し」と「値がnull」を区別する)。`tags` を空でない配列で送った場合はCLIの `edit --tag` と同様に全置換とする。
- エラー応答は `{"error": "<CLIと共通の文言>"}` の形式で返す。存在しないIDに対する操作は `404`、バリデーションエラーは `400`、ロック取得失敗は `503` を返す。

### 8.3 ローカルサーバの安全性

- **バインドアドレス**: `127.0.0.1` にのみバインドする(`0.0.0.0` 等の外部公開は行わない)。
- **DNS rebinding対策**: すべてのリクエストで `Host` ヘッダを検証し、`127.0.0.1:<port>` または `localhost:<port>` 以外の場合は `403` を返す。
- **CORS**: `Access-Control-Allow-Origin` 等のCORS関連ヘッダは一切付与しない。付与するとHostヘッダ検証・CSRFトークンの意味がなくなるため。
- **CSRF対策**: サーバ起動時に `secrets.token_hex(32)`(標準ライブラリ `secrets`。8章冒頭の「追加パッケージは用いない」という制約に合致し、暗号学的に安全な乱数を生成できるため採用)でトークンを生成し、状態変更系リクエスト(`POST`/`PATCH`/`DELETE`)には、カスタムヘッダ `X-Todo-Token` にこのトークンを付与することを必須とする。トークンはサーバが配信する同一オリジンのHTMLの `<meta name="todo-csrf-token" content="...">` に埋め込み、URLのクエリパラメータには含めない(ログ・Referrer・ブラウザ履歴への残留を避けるため)。カスタムヘッダを要求することで、CORS未許可の他オリジンからのブラウザ経由の状態変更は(プリフライトに阻まれ)構造的に不可能になる。フロントエンドJSはページ読み込み時にこの `meta` タグを読み取り、以降のリクエストのヘッダに付与する。テストコードも同様に `GET /` でHTMLを取得し `meta` タグをパースすることで正規のトークンを得られる(テスト可能性.mdの指摘への対応、6章・9章参照)。
  - **埋め込み方式(契約)**: `todo/static/index.html`(8.1節)は `<meta name="todo-csrf-token" content="__CSRF_TOKEN__">` という**固定のプレースホルダ文字列**`__CSRF_TOKEN__`を静的にファイルへ書いておく。`todo/server.py` はこのHTMLファイルを配信する際、文字列 `__CSRF_TOKEN__` を起動時に生成した実際のトークン値へ単純な文字列置換で差し替えてから返す(テンプレートエンジンは使わない)。このプレースホルダ文字列を両者の合意事項として本設計書で確定することで、`index.html`(実装-4)と`server.py`(実装-3)のどちらの構造が先に決まるかを待たずに並行して着手できる(critic-agentの指摘への対応、6章参照)。
- **ポート番号**: `todo serve --port 0` を指定すると、OSに空きポートを動的に割り当てさせる(Pythonの`socket`で`port=0`をbindした場合の標準動作)。これは人がコマンドラインから起動する場合の使い方であり、この場合サーバは実際に割り当てられたポート番号を起動時に標準出力へ1行で出力する(例: `起動しました: http://127.0.0.1:54321`)。一方、結合テスト・同時実行テスト(9章)はプロセスを起動して標準出力を読み取るのではなく、8.4節の `create_server(port=0)` を直接呼び出し、戻り値の `server_port` から実際のポート番号を取得する。ポート取得方法をこのように使い分けることで、固定ポート8765の奪い合いによるテストの不安定化を避ける。
- **同時実行**: 4章の「CLIとブラウザ画面での共有」を参照。`threading.Lock` によるプロセス内直列化と、ファイルロックによるプロセス間排他を組み合わせる。

### 8.4 `todo serve` の起動インターフェース(契約)

`todo/__main__.py`(実装-2、`serve`サブコマンドのディスパッチ)は `todo/server.py`(実装-3)が提供する以下の公開関数のみを呼び出す。

```python
# todo/server.py
def create_server(host: str = "127.0.0.1", port: int = 8765) -> http.server.HTTPServer:
    """ThreadingHTTPServerインスタンスを生成してbindするが、serve_forever()は呼ばない(ブロックしない)。
    port=0の場合はOSが空きポートを割り当てる。実際に割り当てられたポートは
    戻り値の server.server_port から取得できる。"""

def run_server(host: str = "127.0.0.1", port: int = 8765) -> None:
    """create_server()でサーバを生成し、実際のポート番号を標準出力へ出力してから
    serve_forever()でフォアグラウンド起動し、ブロックする(8.3節参照)。"""
```

`todo/__main__.py` の `serve` サブコマンドは `run_server(port=args.port)` を呼ぶだけでよい。実装-2はこのシグネチャに対してコードを書くだけでよく、`run_server`/`create_server` の中身(HTTPサーバの実装そのもの)の完成を待つ必要はない。このシグネチャを本設計書で先に確定することで、実装-2と実装-3(担当B・C)は実装-1の完了後、互いの完成を待たずに並行して着手できる(critic-agentの指摘への対応、6章参照)。

**テストからの停止方法**: `run_server` はブロックするため、9章の結合テスト・同時実行テストは代わりに `create_server` を使う。テストコードは `httpd = create_server(port=0)` でサーバを生成し、`httpd.server_port` で実際のポートを取得し、`threading.Thread(target=httpd.serve_forever, daemon=True)` で別スレッド起動し、テスト終了時に `httpd.shutdown()`(`serve_forever`のループを止める)と `httpd.server_close()`(ソケットを閉じる)を呼んでクリーンに後片付けする。これにより、複数のテスト関数を順に実行してもスレッド・ソケットがリークしない(critic-agentの指摘への対応、6章参照)。

## 9. テスト計画

- **単体テスト**: ストレージ層(読み込み・書き込み・アトミック性・ロック取得/解放・stale lock判定)、バリデーション(`text`/`due`/`priority`/`tags`)、ID採番ロジック(`max(next_id, 最大id+1)`)を対象とする。
- **CLI結合テスト**: 既存コマンド(`add`/`list`/`ls`/`done`/`rm`)の出力が変更前と完全一致することを回帰テストで担保する。加えて、新規コマンド(`edit`/`undone`)・新規オプション(`--due`/`--priority`/`--tag`/`--clear-due`/`--clear-priority`/`--clear-tags`/`--sort`/`--search`/`--status`/`--due-before`/`--due-after`)の正常系・異常系(不正な日付・不正な優先度・存在しないID・全項目省略の`edit`・値指定と`--clear-*`の同時指定など)を確認する。
- **互換性テスト**: 新フィールド(`due`/`priority`/`tags`)を持たない既存の `todo.json` を読み込んでも、追加操作なしでは元のファイル内容(未知キーを含む)が変化しないことを確認する(ラウンドトリップ安全性)。
- **サーバ結合テスト**: `todo serve` を起動し、API経由の `GET`/`POST`/`PATCH`/`DELETE` がCLIと同一のバリデーション・エラーメッセージ・ID採番を用いることを確認する。加えて、`Host` ヘッダ不正時の `403`、`X-Todo-Token` 欠落・不一致時の拒否、CORSヘッダが付与されないことを確認する。
- **同時実行テスト**: CLIプロセスと `todo serve` を同時に動かし、あるいは `todo serve` に対して並行リクエストを送り、`todo.json` が破損しないこと・データが失われないことを確認する(可能であればマルチスレッドからの同時書き込みをシミュレートする結合テストを用意する)。

### テストを決定的・隔離された状態で実行するための指針(テスト可能性.mdの指摘への対応)

| 依存の種類 | 固定・注入・隔離の方法 |
|---|---|
| 現在時刻(`created_at`/`completed_at`) | ストレージ層の `now()` を `storage.now()` の形でmonkeypatchし、固定した日時を返させてからアサートする(3章参照)。 |
| 現在日付(期限切れ判定、5.9節) | 同様にストレージ層の `today()` をmonkeypatchで固定する。相対日付を都度計算する必要はない。 |
| CSRFトークン | サーバのトップページ(`GET /`)を取得し、`<meta name="todo-csrf-token">` をパースして正規のトークンを得る(8.3節参照)。 |
| ポート番号・サーバのスレッド/ソケットの後片付け | テストは `todo serve` をプロセスとして起動せず、8.4節の `create_server(port=0)` を直接呼び出す。戻り値の `server_port` から実際のポート番号を取得し、`serve_forever`をdaemon threadで動かし、テスト終了時に`httpd.shutdown()`と`httpd.server_close()`で明示的に停止・解放する。これにより、固定ポート8765の奪い合いと、複数テスト実行時のスレッド・ソケットのリークの両方を避ける(8.3節・8.4節参照)。 |
| 保存場所(既定の `.todo-data/`) | テストは環境変数 `TODO_HOME` を一時ディレクトリ(例: `tempfile.TemporaryDirectory()`)に設定してから実行し、リポジトリ直下の実データ(`.todo-data/todo.json`)に触れないようにする(4章参照)。 |
| stale lock判定の待ち時間(10秒) | 環境変数 `TODO_LOCK_STALE_SECONDS` をテスト内で短い値(例: 1秒未満)に設定し、10秒待たずにstale lock判定のテストを行う(4章参照)。 |
| PIDの存在確認 | OSのプロセス管理をモックする必要はなく、明らかに存在しない大きなPID値(例: OS上限に近い値)をロックファイルに書き込むことで「PIDが存在しない」ケースを決定的に再現できる。 |

## 10. 実装の段取り(次フェーズ、未着手)

本設計書は設計のみであり、実装はまだ行わない。実装フェーズは以下のファイル単位で分担する。**1ファイルにつき担当者は1人に確定**し、テストファイルも同様に1人の担当とする(実装着手.mdの指摘6・7への対応)。

### ファイルと担当

| ファイル | 内容 | 担当 |
|---|---|---|
| `todo/storage.py` | データモデル・読み込み/書き込み・ラウンドトリップ安全性(3章)、ロック(ファイルロック+`threading.Lock`、公開関数 `acquire_lock()`/`LockGuard`)、`get_storage_dir()`(`TODO_HOME`対応)、`now()`/`today()`、`add`/`edit`/`done`/`undone`/`rm`本体とバリデーション(4章「ロック機構の置き場所とバリデーションの一本化」参照) | 実装担当A |
| `tests/test_storage.py` | 上記の単体テスト(ロック・stale lock・ラウンドトリップ安全性・バリデーション・時刻固定を含む、9章) | 実装担当A |
| `todo/cli.py` | コマンドライン引数の定義・パース(`add`/`list`/`done`/`rm`/`edit`/`undone`/`serve`の各サブコマンド、2章・5章)、出力文言の組み立て(`_format_item`等)。`todo/storage.py` の関数を呼び出すのみで、バリデーションロジック自体は持たない | 実装担当B |
| `todo/__init__.py` / `todo/__main__.py` | パッケージ初期化・エントリポイント(`todo serve` を含むサブコマンドディスパッチ) | 実装担当B |
| `tests/test_cli.py` | CLIの結合テスト(既存コマンドの出力完全一致の回帰テスト、新規コマンド・新規オプションの正常系/異常系、9章) | 実装担当B |
| `todo/server.py`(新規) | `todo serve` 本体: `ThreadingHTTPServer`ベースのHTTPサーバ、8.2節のAPI(`GET`/`POST`/`PATCH`/`DELETE`)、8.3節の安全性対策(バインドアドレス・Hostヘッダ検証・CORS非付与・CSRFトークン生成/検証・`--port 0`対応)、`threading.Lock`によるプロセス内直列化。`todo/storage.py` の公開関数(`acquire_lock()`/`add()`/`edit()`等)を直接呼び出す | 実装担当C |
| `todo/static/index.html`(新規) | 8.1節の画面のHTML構造(フィルタバー・追加フォーム・一覧テーブル・エラー表示欄)、CSRFトークンの `<meta>` タグ埋め込み | 実装担当C |
| `todo/static/app.js`(新規) | フロントエンドのロジック(API呼び出し、`X-Todo-Token`ヘッダ付与、インライン編集、エラー表示欄への描画) | 実装担当C |
| `todo/static/style.css`(新規) | 8.1節の画面のスタイル | 実装担当C |
| `tests/test_server.py`(新規) | `todo serve` の結合テスト(8.2節APIの正常系/異常系、8.3節の安全性対策、9章のサーバ結合テスト・同時実行テスト) | 実装担当C |

### タスクと依存関係

| タスク | 内容(対応ファイル) | 担当 | 依存 |
|---|---|---|---|
| 実装-1 | `todo/storage.py` / `tests/test_storage.py` | 実装担当A | なし |
| 実装-2 | `todo/cli.py` / `todo/__init__.py` / `todo/__main__.py` / `tests/test_cli.py` | 実装担当B | 実装-1 |
| 実装-3 | `todo/server.py` / `tests/test_server.py`(バックエンドAPI) | 実装担当C | 実装-1(実装-2には依存しない。バリデーション・ロックは`todo/storage.py`に一本化されているため、実装-2の完了を待たずに着手できる。実装着手.mdの指摘1への対応) |
| 実装-4 | `todo/static/index.html` / `todo/static/app.js` / `todo/static/style.css` | 実装担当C | 実装-3(APIの入出力仕様が固まってから着手) |
| 実装-5 | 全テストの結合実行・9章の結合テスト観点(互換性テスト・同時実行テスト)の最終確認 | 実装担当全員(担当ファイルのテストは各自作成済みのものを、結合時点で全員でレビュー) | 実装-1〜4 |

実装-1(担当A)は他の全実装の前提となるため最初に着手する。実装-2(担当B)と実装-3・4(担当C)は、実装-1の完了後は互いの**完成**を待たずに並行して進められる。ただし以下の2点は、本設計書で確定した最小限の契約(インターフェース)にのみ依存する形にしてある(critic-agentの指摘への対応、6章参照):
- 実装-2の `serve` サブコマンドは、実装-3が提供する `todo.server.run_server(host, port)` という関数シグネチャ(8.4節)にのみ依存する。実装-3側の内部実装が未完成でも、このシグネチャで呼び出すコードは書ける。
- 実装-3のCSRFトークン埋め込みと、実装-4の `index.html` は、`<meta name="todo-csrf-token" content="__CSRF_TOKEN__">` という固定プレースホルダ文字列(8.3節)にのみ依存する。

各実装タスクは、本設計書(DESIGN.md)を唯一の仕様として着手し、設計と実装が乖離した場合は実装担当からdesign-agent(またはこの設計書の後継)にフィードバックし、設計書側を更新することを原則とする。
