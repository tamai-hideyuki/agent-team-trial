# 実施候補一覧

memo.md(4エージェント討議)の内容から、このアプリで実際にやる候補を抽出したもの。コードとDESIGN.mdはまだ変更しない。各候補に根拠(ファイル名:行番号、または「未確認」)とスコープ判定を付す。

## A. 現在のスコープ内で実施できる候補(DESIGN.md 7章の変更不要)

### A1. Web UIの期限切れ/優先度の視覚的強調
- 何を変えるか: `todo/static/app.js`・`style.css`に、期限切れ行への強調クラス付与、優先度バッジ表示を追加する。
- 根拠:
  - `todo/storage.py:350-362` `is_overdue()`は`done`を見ない設計(コメントにも明記: 「done(完了)かどうかはここでは考慮しない(呼び出し側の責務)」)。フロント実装時は`item.overdue && !item.done`の組み合わせが必須(未確認: 現状の`app.js`側で既にこの組み合わせ済みかどうかは未確認)。
  - `todo/server.py`のAPIレスポンスに`overdue`・`priority`が既に含まれているかは**未確認**(該当箇所を今回のコード確認では特定できず)。
- スコープ: 現在のスコープ内(表示のみの変更、DESIGN.md 5章9項の「色や記号でなくテキストで表現」という既存方針との整合性は要検討)。

### A2. 一覧0件時・初回利用者向けのヒント表示
- 何を変えるか: `todo/cli.py:74`「TODOはありません」の後に、`todo add`の使い方を1行添える。
- 根拠: `todo/cli.py:74` 現状は「TODOはありません」のみ。
- スコープ: 現在のスコープ内。

### A3. Web検索のIME入力時のちらつき対策
- 何を変えるか: `todo/static/app.js:296`の`filterSearch`の`input`イベントにデバウンス、または`compositionstart`/`compositionend`を考慮する。
- 根拠: `todo/static/app.js:296` `filterSearch.addEventListener("input", refresh)`。他のフィルタ(`app.js:294`)は`change`イベントを使用しており、`input`はキー入力ごとにAPIへリクエストを送る。
- スコープ: 現在のスコープ内。

### A4. `edit --tag`の結果メッセージにタグ一覧を含める
- 何を変えるか: `todo/cli.py:106`の編集成功メッセージに、変更後のタグ一覧を追記する。
- 根拠: `todo/storage.py:581-644` `edit()`の`--tag`は既存タグ配列を全置換(追記でない、docstringにも明記)。`todo/cli.py:106`の成功メッセージは`text`のみ表示し、タグの変更結果を表示しない。
- スコープ: 現在のスコープ内。

### A5. server.pyのルーティング共通化(Host検証・CSRF検証の重複解消)
- 何を変えるか: `_valid_host()`/`_check_csrf()`の呼び出しパターンを共通化する。
- 根拠: `todo/server.py:157-259` `do_POST`/`do_PATCH`/`do_DELETE`それぞれで`_valid_host()`(157-158行, 188-189行, 246-247行)→`_check_csrf()`(164行, 197行, 255行)という同じ3行パターンが繰り返されている。
- スコープ: 現在のスコープ内(内部実装の整理のみ)。

### A6. storage.pyのパッケージ分割
- 何を変えるか: `todo/storage.py`(665行)を`todo/storage/`パッケージ化し、`lock`/`validation`/`repository`/`queries`等に分割する(振る舞いは変えない)。
- 根拠: `todo/storage.py`は665行(`wc -l`で確認済み)で、ロック機構・バリデーション・CRUD・検索/ソートが単一ファイルに同居している。
- スコープ: 現在のスコープ内(内部構造のリファクタリングのみ、外部の振る舞いは変えない)。

### A7. CI(GitHub Actions等)の導入
- 何を変えるか: `.github/workflows/`にテスト自動実行のワークフローを追加する。
- 根拠: `.github/workflows`は存在しない(確認済み: `ls`でNo such file or directory)。`tests/`配下に163件のテストが存在する(`grep -c "def test_"`で確認済み)がCIでは実行されていない。
- スコープ: 現在のスコープ内(リポジトリの運用面の変更、アプリのコード自体は変わらない)。

### A8. Web UIフォームのレスポンシブ対応
- 何を変えるか: `todo/static/index.html`の`add-form`が狭い画面で窮屈にならないようにする。
- 根拠: `todo/static/style.css:23-25` `.add-form`には既に`display: flex; flex-wrap: wrap;`が指定済み。狭い画面での実際の見え方(要素が意図通り折り返るか)は**未確認**(ブラウザでの実機確認をしていない)。
- スコープ: 現在のスコープ内。**優先度の見直しが必要な候補**(前回討議ではCSSが未対応という前提で議論されたが、実際には`flex-wrap`が既に指定されている。実害があるかどうかから確認すべき)。

## B. 既存スコープを変更する必要がある候補(DESIGN.md 7章の記載と抵触)

DESIGN.md 7章(スコープ外)には以下が明記されている:
- `DESIGN.md:404` 「`rm` の確認プロンプト・ゴミ箱機能」
- `DESIGN.md:409` 「`text`/`due`/`priority`/`tags`の変更履歴の保持・直前の値への取り消し(undo/revert)機能」(`done`/`undone`のみ両方向対応)
- `DESIGN.md:403` 「サブタスク、繰り返しタスクなどの高度な機能」
- `DESIGN.md:405` 「SQLiteなど本格的なデータベースの利用」
- `DESIGN.md:407` 「自動バックアップ・自動復旧機能」

### B1. ゴミ箱機能(`remove`のソフトデリート化+`restore`+`purge`)
- 何を変えるか: `todo/storage.py:647-663`の`remove()`を、配列からの即時削除(662行目`data["items"] = [item for item in data["items"] if item["id"] != item_id]`)から`deleted_at`フラグを立てる方式に変更し、`restore`/`purge`コマンドを新設する。
- 根拠: `todo/storage.py:647-663`で`remove()`は即座に配列から除去する実装であることを確認済み。`DESIGN.md:404`で明示的にスコープ外とされている。
- スコープ: **DESIGN.md 7章の変更が必要**(404行目の削除・修正)。

### B2. 汎用1手undo機能
- 何を変えるか: `save()`実行直前の状態を1世代保持し、`todo undo`で直前の操作を取り消せるようにする。
- 根拠: `DESIGN.md:409`で「`done`/`undone`のみ両方向の操作を提供し、それ以外のフィールドはundo/revert機能を提供しない」と明記されている。
- スコープ: **DESIGN.md 7章の変更が必要**(409行目の修正)。B1(ゴミ箱)と技術的に関連するが、独立して実施可能(ゴミ箱がなくてもundoは実装できる)。

### B3. 繰り返しタスク
- 何を変えるか: `--repeat daily/weekly/monthly`等のオプションで、完了時に次回分を自動生成する。
- 根拠: `DESIGN.md:403`で「サブタスク、繰り返しタスクなどの高度な機能」が明示的にスコープ外とされている。
- スコープ: **DESIGN.md 7章の変更が必要**(403行目の修正)。データモデルの拡張(繰り返しルールとインスタンスの関連付け)も伴う。

### B4. SQLite移行
- 何を変えるか: `.todo-data/todo.json`のファイルベース保存をSQLiteに置き換える。
- 根拠: `DESIGN.md:405`で「SQLiteなど本格的なデータベースの利用」が明示的にスコープ外とされている。
- スコープ: **DESIGN.md 7章の変更が必要**(405行目の修正)。ストレージ層全体の書き換えを伴う大規模変更。

### B5. 自動バックアップ機構
- 何を変えるか: 保存の都度、直前バージョンを別ファイルに退避する。
- 根拠: `DESIGN.md:407`で「自動バックアップ・自動復旧機能」が明示的にスコープ外とされている。
- スコープ: **DESIGN.md 7章の変更が必要**(407行目の修正)。B2(1手undo)と機能的に重複する可能性がある。

---

## 採点・議論の結果

4人(UX/アーキテクチャ/テスト・信頼性/機能ロードマップ)がそれぞれ独立に、全13候補を4基準(使う人にとっての価値/作る手間の小ささ/壊すリスクの低さ/他の候補の前提になるか、各1〜5点)で採点。点が割れた候補(A6, B1, B2, B3, B5)について直接議論を2ラウンド実施し、2ラウンド目で順位に変化がなかったため終了(ユーザー指定の終了条件を満たす)。

### 独立採点(議論前、4人合計/20点満点)

| 候補 | 価値+手間+リスク+前提の合計(4人) | 平均 |
|---|---|---|
| A7 | 15, 15, 13, 14 | 14.25 |
| B1 | 13, 16, 15, 13 | 14.25 |
| A4 | 13, 14, 13, 14 | 13.5 |
| A1 | 14, 14, 13, 12 | 13.25 |
| A2 | 13, 14, 13, 13 | 13.25 |
| A8 | 13, 13, 13, 12 | 12.75 |
| A3 | 13, 13, 12, 12 | 12.5 |
| B5 | 11, 11, 14, 14 | 12.5 |
| B2 | 11, 9, 13, 14 | 11.75 |
| A6 | 12, 10, 13, 9 | 11 |
| A5 | 10, 11, 9, 9 | 9.75 |
| B3 | 8, 11, 9, 10 | 9.5 |
| B4 | 5, 5, 4, 4 | 4.5 |

## 順位とおすすめ(スコープ内候補、A1〜A8, A5除く)

議論の結果、1位はA7とB1が同点タイのままだが、これは「優劣の対立」ではなく「両方とも最優先で、依存関係がないため並行/A7先行で着手すべき」というシグナルであるという結論で4人が合意した。

1. **A7. CI(GitHub Actions等)の導入** — 平均14.25点。ユーザー価値は直接的には低い(2点前後)が、以降のリスクを伴う変更(B1等)を安全に進めるための前提性が最も高いと判定された。
2. **A4. `edit --tag`の結果メッセージにタグ一覧を含める** — 平均13.5点。コストほぼゼロで、タグ全置換という事故りやすい仕様への気づきを提供できる。
3. **A1. Web UIの期限切れ/優先度の視覚的強調**、**A2. 初回利用者向けヒント表示** — ともに平均13.25点。低コストで実施可能。
4. **A8. Web UIフォームのレスポンシブ対応** — 平均12.75点。ただし`style.css:23-25`で既に`flex-wrap: wrap`が指定済みであることが判明しており、実害の有無を確認してから着手すべき(未確認: 実際の狭い画面での見え方)。
5. **A3. Web検索のIME入力時のちらつき対策** — 平均12.5点。
6. **A5. server.pyのルーティング共通化** — 平均9.75点。ユーザー価値はゼロだが、B1等でエンドポイントが増える前に整理しておく意味はある。

**A6(storage.pyのパッケージ分割)について**: 議論の結果、「candidates.md記載通りのフル分割」として独立候補で評価するなら平均11点相応だが、実際に必要なのはB1/B2の実装に必要な最小限の抽出(`atomic_io.py`相当、既存の`save()`ロジックを変えずに切り出すのみ)であり、これは独立候補ではなくB1/B2のタスク内訳の一部として扱うべき、という結論で4人が合意した。したがって順位表には含めず、実装時の前処理として扱う。

## スコープを変える候補の順位とおすすめ(B1〜B5、DESIGN.md 7章の変更が必要)

1. **B1. ゴミ箱機能(`remove`のソフトデリート化+`restore`+`purge`)** — 平均14.25点(A7と同点タイ)。**推奨着手順序: A7(CI)の後に着手**(CIをB1の変更を検知する安全網として先に整備する)。
2. **B3. 繰り返しタスク** — 平均9.5点だが、B1が先行実装されればリスク評価が引き上がる(誤生成した繰り返しタスクを`rm`→`restore`で個別に取り消せるようになるため)。**推奨: B1の後に着手を検討**。ただしデータモデル拡張という実装コスト自体はB1の有無と無関係に発生する。
3. **B2. 汎用1手undo** — 独立採点は11.75点だったが、議論で価値3/手間2/リスク2/前提3=10点に収束。**B2はedit/clear_*系の上書きミス(text/tags全置換、`--clear-due`等)に対して独自の価値を持つが、B1導入によって『削除の取り消し』という頻度が高いと想定される失敗モードが先にカバーされるため、単独で優先実装する必要性は下がる**(edit系ミスの発生頻度・深刻度が削除ミスほど高くないという前提に立てば、という条件付き)。実装するとしてもCLI/Web同時利用時の巻き添え消去リスク(下記の残った懸念を参照)への対策が前提。
4. **B5. 自動バックアップ機構** — 独立候補としては見送り。B2(1手undo)実装時にほぼ無償で得られる副産物として統合する。
5. **B4. SQLite移行** — 平均4.5点で最下位。4人とも「現状の運用規模では価値がなく、コスト・リスクだけが最大」という点で一致。時期尚早。

## 1位(A7・B1)を選んだ理由

- A7とB1はどちらも「他の候補の前提になるか」の点数が高く(A7: 平均3.75、B1: 平均4.25)、単体の価値だけでなく、以降の変更(特にB系候補)を安全に進めるための土台という性質が共通していた。
- 議論を通じて、A7とB1には技術的な依存関係がないことが確認され(arch-agent・test-agentの議論で「優劣ではなく順序の話」と整理)、両方を最優先候補として扱うことに4人とも合意した。
- B1は「使う人にとっての価値」が4人中3人から4〜5点という高評価を得ており(唯一arch-agentが3点)、討議全体(memo.md)でも最も作り込まれた機能である点も後押しとなった。

## 残った反対意見・懸念(未解決のまま記録)

- **B1実装時のリスク(arch-agentが当初1位に反対した根拠)**: `remove()`という中核操作の変更と、`list_items()`の共有フィルタ経路への暗黙の除外前提の追加は、既存163件のテストでは検知できない次元のバグ(trashed項目の再浮上等)を生むリスクがある。この懸念自体は解消されておらず、「A7(CI)を先に整備することでリスクを相殺する」という前提のもとでB1の実施に合意した、という条件付きの決着である。
- **B2見送りの残存リスク(roadmap-agentが申し送り)**: B2を見送ると、「`--clear-due`/`--clear-priority`/`--clear-tags`で元の値が完全に失われる」という事故はB1(削除の取り消し)ではカバーされない。将来この点が実害として顕在化した場合の再検討候補として記録しておく。
- **採点方法自体への指摘(test-agent・roadmap-agentが提起)**: 「他の候補の前提になるか」を高く採点しながら「今すぐやる理由にはならない」と結論するケース(A6等)があり、4基準の単純合計だけでは「今すぐ着手すべき順位」と「いずれやるべき重要度」を区別しきれないという構造的な指摘があった。今回はA6を独立候補から外す(実装の一部として扱う)ことで個別に解決したが、今後同種の候補が出た場合は同じ整理が必要になる可能性がある。
