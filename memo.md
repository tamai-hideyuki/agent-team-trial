# TODO管理システム改善に関する4視点討論メモ

- 日時: 2026-09-12
- 参加者(役割): UX / アーキテクチャ・コード品質 / テスト・信頼性 / 機能ロードマップ の4視点エージェント
- 形式: 各視点が独立にリポジトリをレビュー→意見出し(ラウンド1)→team-lead経由で相互の質問に回答(ラウンド2)→エージェント同士が直接メッセージで討議し合意形成(ラウンド3)

## 総括

現状のTODO管理システムは「単一ユーザー・単一マシン・依存関係ゼロ」というスコープをDESIGN.mdで明文化しており、4視点とも**このスコープ限定自体は良い判断**という点で一致した。議論を通じて、優先して着手すべきは新機能追加よりも「今ある機能の土台(データ整合性・テスト・視認性)を固めること」という結論でまとまった。

## 視点別の主な指摘

### 1. UX
- 良い点: 統一されたフィードバック文言、テキストベースの期限切れ表示(色に依存しない堅実さ)、CLI/Web間でエラー文言が共通。
- 改善提案:
  - Web UIの入力フォームのレスポンシブ対応(狭い画面での窮屈さ)
  - 一覧の視認性向上(期限切れ・優先度の視覚的強調。CLIの「色を使わない」方針をWebにまで引き継ぐ必要はない)
  - 初回利用者への導線不足(使い方のヒントがない)
  - Web検索のIME入力時のちらつき(input毎リクエスト、デバウンス/composition考慮なし)
  - `edit --tag`の全置換挙動が事故りやすい(結果表示に変更後タグを含めるべき)

### 2. アーキテクチャ・コード品質
- 良い点: storage層とCLI/serverの責務分離、アトミック書き込み+ファイルロック、CSRF/Hostヘッダ検証などローカル用途に見合ったセキュリティ配慮、依存関係ゼロの維持。
- 改善提案:
  - storage.py(665行)を関心事別(ロック/バリデーション/CRUD等)に分割
  - ファイルベースJSON+ロック方式の限界を見極め、SQLite移行の判断基準(閾値)を今のうちに用意
  - server.pyのルーティング重複(Host検証・CSRFチェック)をテーブル駆動/共通化
  - CLIとserverで非対称なロック戦略(二層ロック)をstorage.py側に一元化

### 3. テスト・信頼性
- 良い点: 163件のテストが破損データ・ロック競合・時刻依存を良くカバー、アトミック書き込みで途中クラッシュに強い設計。
- 改善提案:
  - 実プロセス間の真の並行アクセステスト(現状はプロセス内ロックのみ検証)
  - tmpファイル残存など異常終了からの復旧検証
  - item単位のデータバリデーション(現状はトップレベルのみ)
  - CI(GitHub Actions等)未導入 → 自動テストゲートが無い
  - バックアップ/世代管理の欠如(誤操作からの復旧手段なし)

### 4. 機能ロードマップ
- 良い点: 機能が過不足なく実用的、DESIGN.mdによるスコープの歯止めが機能拡大を防止。
- 改善提案(優先度順):
  - 繰り返しタスク(中): 定型作業の入力コスト削減。ただし要スコープ改訂。
  - リマインダー通知(低〜中): OS通知止まりに限定し常駐プロセス化は避ける。
  - iCalエクスポート(低): 片方向出力のみで既存モデルに影響小。
  - サブタスク・チーム/複数ユーザー機能: **非推奨**。複雑度増大がツールの価値(軽量・シンプル)を毀損する。

## 討論で解消された論点(ラウンド2)

- **SQLite移行は誰の要請で判断すべきか**: 機能ロードマップ側(複数ユーザー・同期)からの要請は無いと確認。移行の是非は純粋にテスト・信頼性/アーキテクチャ側の実データ(並行アクセステストの結果)で判断すべき、という合意に至った。「単一プロセス・単一ファイル」の前提は当面変更しない。
- **Web UIの視覚的強調はアーキテクチャ上問題ないか**: `overdue`・優先度はすでにAPIレスポンスに含まれており、フロント(CSS/JS)のみで完結する変更と確認。実装障壁なし。
- **繰り返しタスクを入れる場合のデータモデル**: 単純追記では不十分。「繰り返し定義」と「生成済みインスタンス」を分離するフィールド設計が必要、かつテスト側の「item単位バリデーション」「バックアップ機構」が前提条件になるという合意。
- **ブラウザ限定の通知UIは許容か**: UXは条件付きで許容(通知の元データはCLIでも同じ形で見えることを維持する)。
- **UI層の回帰確認の現状**: レスポンシブ・IME・視覚的ハイライトは自動テスト対象外で手動確認頼み。E2E/スクリーンショット比較の導入は費用対効果を見て検討。

## エージェント間の直接討議で成立した合意(ラウンド3)

team-leadを介さず、エージェント同士が直接メッセージをやり取りして次の4件で具体的な合意に達した。

**1. arch-agent × test-agent — SQLite移行の判断軸**
「閾値を決めること」と「実際に移行すること」を分離。閾値の明文化(DESIGN.mdへの記載)は即実施、実際の移行は保留とし、test-agentが追加する実プロセス間並行テストの結果(競合の失敗率・待ち時間)次第で判断する。判断軸は「item数」単独ではなく「同時書き込み衝突の頻度」を主軸にする。

**2. test-agent × roadmap-agent — 土台整備と機能追加の順序**
新機能(繰り返しタスク・リマインダー等)より先に、①item単位のバリデーション強化 → ②バックアップ/復旧機構の整備、を行う順序で合意。新機能はいずれも優先度「中」以下・任意であり、既存データの信頼性を優先すべきという判断で異論なし。

**3. arch-agent × ux-agent — Web UIの視覚的強調の実装方針**
`overdue`・`priority`は既にAPIレスポンスに含まれておりAPI変更は不要、CSS/JSのみで完結。実装方針は「期限切れ行への`is-overdue`クラス付与」「優先度バッジ」。
**重要な指摘**: `storage.is_overdue`は完了済み(`done`)かどうかを見ない設計のため、フロント側で`item.overdue && !item.done`と組み合わせないと、完了済みタスクの行にまで警告色が付いてしまう(見落とし防止のはずが逆に混乱を招く)。実装時に必ず反映すること。ダークモード対応は今回スコープ外。

**4. roadmap-agent × ux-agent — 新フィールドの一覧表示方針**
繰り返しタスクを将来実装する場合、一覧には「(繰り返し)」等の最小表記のみ表示し、頻度・曜日等の詳細は`edit`実行時/Web編集モードでのみ表示する。さらに一般方針として「新フィールドは既定非表示、`--verbose`的オプションで段階的開示」という拡張ルールにも合意し、既存の付記フォーマット(期限・優先度・タグ)の可読性を将来にわたって守る。

## 優先アクション（4視点の合意ベース、直接討議の結果を反映）

1. **信頼性の土台固め(最優先)**: 実プロセス間の並行アクセステスト追加 → item単位のバリデーション → バックアップ/復旧機構、の順で着手。SQLite移行の判断閾値もこのタイミングでDESIGN.mdに明文化する。
2. **Web UIの視認性・レスポンシブ改善**: 期限切れ/優先度の視覚的強調(`item.overdue && !item.done`条件を必ず使用)、フォームのレスポンシブ対応、IME入力時の検索デバウンス。API変更不要ですぐ着手可能。
3. **コード整理**: storage.pyの分割、server.pyのルーティング共通化。機能追加の前にやっておくと後続作業がしやすい。
4. **小さな導線改善**: 初回利用者向けのヒント表示、`edit --tag`全置換の結果可視化。
5. **機能追加は土台整備の後**: 繰り返しタスク・リマインダー(OS通知)・iCalエクスポートは、上記1〜3が済んだ後にDESIGN.md改訂とセットで検討。一覧表示は最小表記+`--verbose`的開示のルールに従う。サブタスク・複数ユーザー機能は追加しない方針を維持。

## 継続検討事項(合意はしたが実行時に詰める点)

- 実プロセス間並行テストの具体的な指標(失敗率・待ち時間のしきい値)をDESIGN.mdの「SQLite移行トリガー」としてどう明記するか。
- 繰り返しタスクを実装する際の「recurrence定義とインスタンスの親子関係」の具体的なデータ構造。
- ダークモード対応やCSS設計全体の方針(今回は視覚的強調のみでスコープ外とした)。

---

# 拡張討議: 「この世で完成されたTODOリストとは何か」

- 形式: 4視点エージェントがこのリポジトリの改善提案という枠を外し、それぞれWebSearchで外部の生産性理論・プロダクト事例・学術的知見を調査した上で、直接メッセージで意見をぶつけ合った。
- 参照した外部知見: GTD(David Allen)、Time Blocking、Bullet Journal、TODOリスト批判論、RFC 5545(iCalendar VTODO)、Local-first software(Ink & Switch)、org-mode、Zeigarnik効果、生産性アプリの離脱率調査、禅/ミニマリズム論など。

## 視点別の最終仮説

### UX視点
> 「完成されたTODOリスト」は機能の多さでも少なさでも測れない。測れるのは**「ユーザーが今この瞬間に処理を強いられる情報量の少なさ」**だけである。

- 機能自体は裏に持ってよいが、常時画面に出すか・オンデマンドでしか出さないかという「表示設計」が本質。
- システムがユーザーの意思決定を勝手に代行する(例: 気づかれない自動アーカイブ)ことは信頼を壊す方向に働く。整理・棚卸しの"摩擦"はむしろ意図的に残し、自動化すべきは「見せ方」であって「判断」ではない(Bullet Journalの"migration"思想)。
- 参照: [Why to-do lists don't work](https://medium.com/swlh/why-to-do-lists-dont-work-568063af5067) / [Amplenote: classic to-do lists always fail](https://www.amplenote.com/classic_todo_lists_always_fail) / [8 Reasons Using Todoist for GTD Might Hurt You](https://thedivit.com/reasons-using-todoist-for-gtd-might-hurt-you) / [Reclaim.ai: Time Blocking Guide](https://reclaim.ai/blog/time-blocking-guide) / [Bullet Journal: Migration 101](https://bulletjournal.com/blogs/faq/migration)

### アーキテクチャ視点
> 「完成されたTODOリスト」は機能の集積ではなく、**時間が経っても信頼を裏切らないデータ基盤**である。

- 3条件: (1)永続一意ID(削除・移動・同期を経ても意味を失わない識別子)、(2)削除ではなく状態遷移として設計する(TODOの"完成"はレコード削除ではなく、同じIDのまま状態が変わっていくこと)、(3)移行判断の軸に「性能劣化」だけでなく「データロスト・矛盾が起きる確率」を加える。
- VTODOとVEVENTが同一UIDを共有する兄弟コンポーネントである点は、「TODOは実行段階でカレンダー的形式に翻訳されて消える」という思想と技術的に符合する。
- 参照: [RFC 5545 (iCalendar) VTODO](https://datatracker.ietf.org/doc/html/rfc5545) / [Local-first software (Ink & Switch)](https://www.inkandswitch.com/essay/local-first/) / [org-mode](https://orgmode.org/org.html) / [Why You Hate Every To Do App](https://zapier.com/blog/why-you-hate-every-to-do-list-app/)

### テスト・信頼性視点
> 完成されたTODOリストとは、**「書けば本当に忘れていい」という確信を一度も裏切らないシステム**である。これは静的な完成状態ではなく、使うたびに再達成される動的な信頼関係である。

- データ消失は単なる不具合ではなく、アプリの存在理由そのものの破壊(Zeigarnik効果 — 未完了タスクを「手放してよい」と脳に伝える機能そのものが壊れる)。
- 信頼性は「見えない基盤」であるべきで、UI上の演出(保存インジケータ等)では代替できない。むしろ演出自体が新たな確認コストを生み、逆効果になりうる。
- 「安心感」のような主観的ゴールも、「安心感が壊れる引き金」(データ消失・通知未達・状態の矛盾)という具体的な不変条件に翻訳すればテスト可能。
- 参照: [Why Power Users Abandon Notion, Todoist & Trello](https://unstar.app/blog/productivity-app-reviews-what-power-users-complain-about-2026) / [App Abandonment in 2026](https://www.forasoft.com/blog/article/avoid-app-abandonment-strategies) / [The Zeigarnik Effect](https://www.productiveflourishing.com/p/zeigarnik-effect) / [Mobile App Retention Benchmarks](https://uxcam.com/blog/mobile-app-retention-benchmarks/)

### 機能ロードマップ視点
> 「完成されたTODOリスト」は静的なゴールとして存在しない。「完成」という概念そのものがこの道具の本質と矛盾する。

- 理由は3つ: (1)価値は「認知負荷の外部化」という動的行為にあり、使うたびに再達成されるものだから、(2)真に機能した瞬間、リストは実行への翻訳(カレンダーや単発の行動)に姿を変えて消えていくから、(3)機能を積み上げるほど「存在を意識させない透明性」という理想から遠ざかる逆説があるから。
- 答えがあるとすれば、それは「これを書けば、もう覚えていなくていい」という確信を、道具の存在を意識させずに毎回再現できること。これは足し算(機能追加)ではなく、引き算(何を書かせない/見せないか)と土台の信頼性によってのみ近づける。
- 参照: [Getting Things Done](https://gettingthingsdone.com/) / [Why Your To-Do List Isn't Working](https://www.calendar.com/blog/why-your-to-do-list-isnt-working-and-how-to-fix-it/) / [The Perfect Productivity App Doesn't Exist](https://redeemingproductivity.com/the-perfect-productivity-app-doesnt-exist/) / [Zen Habits: Zen Productivity](https://zenhabits.net/zen-productivity/)

## 4視点が収斂した結論

異なる角度から出発したにもかかわらず、4つの仮説は同一の構造に収斂した。

**「完成されたTODOリスト」とは、機能の網羅性やUIの洗練度では定義できない。それは「これを書けば、もう自分の頭で覚えておかなくていい」という利用者の確信を、道具の存在を意識させることなく、使うたびに裏切らずに再現し続けることである。**

- この確信は**実装の正しさ(アトミック性・排他制御・バリデーション)という見えない土台**によってのみ支えられ、UI上の演出では代替できない(テスト・信頼性 × UX)。
- この確信を壊す最大の要因は「機能不足」ではなく「信頼の一度の裏切り(データ消失・意思決定の無断代行)」である(アーキテクチャ × UX)。
- 機能を追加するほど理想に近づくとは限らず、むしろ「常時見せる情報量を増やさない」引き算の設計判断こそが本質に近い(機能ロードマップ × UX)。
- TODOの"完成"は削除ではなく状態遷移(実行への翻訳)として捉えるべきで、道具としての完成はリストという形式自体を離れた先にある(アーキテクチャ × 機能ロードマップ)。

## 今回の議論から実務に持ち帰れる具体的示唆

1. 自動アーカイブのような「ユーザーの気づかないところでの判断の代行」は避け、`review-stale`的な「気づかせて判断はユーザーに残す」設計にする。
2. 新機能・新フィールドは常時表示しない(オンデマンド表示・`--verbose`的開示)。GTD的な「重み(所要時間・エネルギー・コンテキスト)」フィールドの追加は非推奨。
3. 信頼性への投資(アトミック書き込み・排他制御・バリデーション・バックアップ)を、UI上の「安心感の演出」より優先する。
4. マルチデバイス同期を要件化しない限り、正しさの検証が容易な排他制御(ロック)をCRDTより優先する。
5. データモデルの識別子・状態設計は、将来「削除ではなく状態遷移」で拡張できるよう、今のうちに意識しておく(即座の設計変更は不要)。

---

# 弁証法ラウンド: あえて反対の立場から論破し合う

4視点の収斂した結論が「予定調和」で終わらないよう、担当をローテーションしてお互いの結論に**意図的に反対の立場から論破を試みる**討議を実施した(UX→アーキ、アーキ→テスト、テスト→ロードマップ、ロードマップ→UX)。「論破された部分(撤回)」と「反論しても崩れなかった部分(維持)」を区別することで、単なる意見の足し合わせではなく、削って残った芯を取り出すことを狙った。

## ラウンド1: UX vs アーキテクチャ(データ基盤の正しさへの反論)

**論破の主張**: 「データ基盤の正しさ」はユーザーに一切観測されない自己満足であり、信頼は設計の正しさではなく「事故に遭わなかった経験の蓄積」から来る。個人開発ツールへのロック機構等の重装備は複雑化=バグの温床という過剰投資ではないか。

- **撤回された点**: 「正しい設計→信頼が生まれる」という直接因果は論理の飛躍だったとアーキテクチャ側が認めた。また「これ以上の正しさ投資(ロックのさらなる堅牢化)は不要」という線引きにも同意した。
- **維持された点**: 「UIDを連番でなく永続一意にする」等、実装コストがほぼゼロで後からの変更コストだけ高い設計判断は、規格準拠への自己満足ではなく合理的先行投資として反論が通った。
- **着地点**: 「完成されたTODOリスト」の本体は"データ基盤の正しさ"そのものではなく、**「ユーザーが一度もデータの心配をせずに済む体験」**。正しい設計はそれを支える手段の一つに過ぎず、手段の取捨選択は「後からの変更コストの高低」で較正する(コスト低=今やる、コスト高で今すぐ不要=見送る)。

## ラウンド2: アーキテクチャ vs テスト・信頼性(「見えない信頼性」への反論)

**論破の主張**: 「信頼性を演出しない」は理想論であり、ユーザーは実装の正しさを直接検証できない以上、見える安心材料なしに信頼は醸成されない。「一度も裏切らない」というゼロリスク志向は過剰品質でリリース遅延を招く。「性善説的ユーザー」を前提にしすぎではないか。

- **撤回された点**: 「信頼性は完全に不可視であるべき」という強い主張を撤回。「異常系の演出(保存アイコンが赤くなる等)は不要だが、正常系の即時反映(操作直後に一覧へ反映される)という最小限の証拠提示は必要」に修正。
- **維持された点**: 「実プロセス間並行テストは過剰品質」という批判は退けられた。これは新規のゼロリスク保証の追加ではなく、既存のLockGuard設計が主張通り動くかという未検証ギャップを埋める投資であるという整理にアーキテクチャ側も納得。「性善説批判」も、"試用段階の離脱"と"定着後に静かにデータを失って離脱する"は別フェーズであり、後者は統計に出にくいが軽視すべきでないと押し返された。
- **着地点**: 信頼性は演出でなく「正常系の最小限の証拠」として実装に組み込む。並行テストは既存設計の検証ギャップを埋める妥当な投資。「一度も裏切らない」への投資は無制限ではなく、既存の主張(設計)が実際に成立しているかを検証する範囲に留める。

## ラウンド3: テスト・信頼性 vs 機能ロードマップ(「完成は存在しない/引き算が本質」への反論)

**論破の主張**: 「完成は存在しない」は実務上何のアクションにも繋がらない思考停止的レトリック。「引き算が本質」と言いながら視覚的強調や優先度バッジのような"足し算"を採用しているのは矛盾。GTD/禅からの引用は人間の生産性哲学であり設計原則への直接翻訳は論理の飛躍。「41%のタスクが完了しない」という統計を「リストという形式の限界」とするのも飛躍。

- **撤回された点**: 「引き算が本質」という結論そのものを撤回。個別の合意判断(繰り返しタスクは最小表記のみ、リマインダーはベストエフォート等)は「機能を削る」のではなく「常時表示する情報量だけを絞る」ことで一貫しており、"足し算"の機能(優先度バッジ等)と矛盾していなかったため。
- **維持された点**: 「41%統計」は「review/棚卸し機能の不足」の証拠として合意され、これは既存の期限切れ優先表示と同じ設計思想(注意すべきものだけを表に出す)の延長として整合すると再解釈された。
- **運用ルールとして合意**: 哲学的引用は結論の権威付けではなく、あくまで仮説生成のきっかけとして使う。
- **着地点**: 「足し算(裏側の機能・データ量)は許容するが、常時ユーザーの目に触れる情報量だけは増やさない」という非対称設計の基準に精緻化。

## ラウンド4: 機能ロードマップ vs UX(「摩擦を残すべき/情報量最小化」への反論)

**論破の主張**: 「摩擦を意図的に残す」はBullet Journal的な手書き文化への懐古主義で、自動化という強みの放棄。既に期限切れ自動表示という自動化を歓迎しているのは矛盾。「情報量の少なさ」の追求は単一の(初心者向けの)利用者像への最適化で、パワーユーザーを切り捨てる。「意思決定の代行はダメ」はAI活用等の技術方向性を頭ごなしに否定していないか。

- **撤回された点**: 「情報量ゼロへの漸近」という表現は訂正され、「デフォルトは最小、能動的に掘り下げれば全情報にアクセス可能」という progressive disclosure(`--verbose`案)に精緻化。パワーユーザー対応と情報量最小化は対立しないと訂正。
- **維持された点**: 「矛盾している」という指摘は退けられた。期限切れ自動表示は「元データを一切変えず、都度計算して見せているだけ」の可逆な表示最適化であり、`--sort due`でいつでも素の状態に戻せる。棚卸し・アーカイブのような「データの状態を書き換える」操作の自動化とはレイヤーが違うと反論が通った。
- **精緻化された基準(両者合意)**: 「見せ方」と「判断」を隔てる本質的な線は**可逆性と透明性**。表示の最適化(データ不変・可逆)は自動化を歓迎、状態変更(データ可変・不可逆)は必ず明示的同意が必要。
- **新たに浮上し解決した論点**: リマインダー通知は「データを変えないが、ユーザーの注意資源を不可逆に消費する」という、表示/状態変更のどちらにも当てはまらない第三のカテゴリ。最終的に3分類で合意 — ①表示の最適化(同意不要)、②状態変更(都度の明示的同意が必須)、③注意の割り込み=通知(事前の一括同意で足り、都度同意は不要)。

## 弁証法を経て生き残った"芯"

4ラウンドの論破・再反論を通じて、当初の収斂結論のうち**強すぎる断定(「完全に静的完成は存在しない」「信頼性は完全に不可視であるべき」「引き算が本質」)は現実的な形に修正され**、以下が最終的に生き残った:

1. **本体は体験、設計はその手段**: 「完成されたTODOリスト」の本体は"データ基盤の正しさ"でも"引き算の思想"でもなく、**「ユーザーが一度もデータの心配をせずに済む体験」**。データ設計・テスト・機能取捨選択は、すべてこの体験を支えるための手段であり、手段同士の優先順位は「変更コストの高低」で較正する。
2. **信頼性は最小限だが不可視ではない**: 異常系の演出は不要だが、正常系の即時反映という最小限の証拠提示は必要。ゼロリスク投資ではなく、既存設計の主張が実際に成立しているかの検証(実プロセス間並行テスト等)に留める。
3. **非対称設計の基準は「可逆性」**: 表示の最適化(可逆・データ不変)は自動化を歓迎し常時見せてよい。状態変更(不可逆・データ可変)は都度の明示的同意が必須。通知のような「注意の不可逆消費」は事前の一括同意で足りる第三のカテゴリとして扱う。
4. **統計・哲学的引用は仮説生成の道具であって結論の権威付けではない**: 「41%が未完了」も「禅の思想」も、それ自体が結論を証明するのではなく、検証すべき仮説(review機能の不足など)を思いつくためのきっかけとして扱う。

---

# 継続討議ループ(セッション1〜2/10)

弁証法ラウンドで残った"芯"4点を起点に、「まだ甘い点を見つけて直接議論し、精緻化する」というループをさらに実施した。各セッションで見つかった甘さと、それによる更新を記録する。

## セッション1: 発見された甘さと更新

- **test-agentの発見**: 「非対称設計の基準は可逆性」は"同意を得たか"のみを見ており、"同意がユーザーの真の意図と一致していたか"を見落としていた(例: `todo rm 12`のつもりで`todo rm 1`と誤入力しても、コマンド実行という同意の形式は満たされてしまう)。
  → **信頼性が3層に整理された**: ①クラッシュ耐性(データが消えない) ②バリデーション(意図しない状態にならない) ③人間のミスからの復旧(undo/ゴミ箱)。これまでの議論は①②に偏っており、③が新たな実務的な穴として具体化された。
- **arch-agentの発見**: 「可逆性」を対象操作固有の性質として扱うのは甘く、実際は「可逆にするコストをかけるかどうか」という実装側の選択変数である(`remove`も論理削除+ゴミ箱にすれば低コストで可逆にできる)。
  → 芯#3が**「可逆性は所与でなく実装コストで買える」「同意の要否は"可視性が保証されない不可逆(実質不可逆)"で最終判定する」**に精緻化。可逆にしても誰も気づかず消えるなら体験上は不可逆と同じ、という下限条件が追加された。
- **roadmap-agentの発見**: 「データが変わるか」を分類軸にすると、繰り返しタスクの自動生成(データは書き換わるが、同意の取り方はルール設定時の1回で以降は機械的執行)がどの分類にも綺麗に収まらない。
  → 分類軸を**「新規の裁量(discretion)が発生する回数」**に置き換え。可逆性はこの軸から副次的に導かれる性質(裁量ゼロの機械的執行は可逆に設計すべき、というだけ)であって、軸そのものではないと整理された。
- **具体的な実装案(この時点で合意)**: `remove`はゴミ箱方式(`deleted_at`フラグ+`restore`/`purge`)、`edit`は直近1件のみ戻せる`todo undo`。一括操作(`review-stale --archive-all`)は`isatty()`で対話/非対話を判定し、対話時はプレビュー確認、非対話時は`--yes`必須という標準CLIパターンで「同意の粒度」問題を解消。

## セッション2: 発見された甘さと更新

- **test-agentの発見**: 「removeだけが真に取り返しのつかない操作」という整理は不正確。`edit`のタグ全置換や`clear_due`等も既存値を不可逆に破棄しており、完全に可逆なのは`done`/`undone`のトグルのみ。
  → **取り消し可能性を2層構造に**: (1)汎用スナップショット(`save()`直前の全体状態を1世代だけ退避し`todo undo`で戻す。add/edit/remove/clear_*すべてを同じ仕組みでカバー、実装コスト最小)、(2)`remove`専用の長期ゴミ箱(数日後に気づくケースに対応。スナップショットは1世代のみのため他の操作で即上書きされ長期保護にならないため)。「直近の事故」と「数日単位の事故」は要求される時間軸が異なり1機構に統合できない、という結論。
- **roadmap-agentの検証**: 「裁量の発生回数」軸を未検証機能に適用。繰り返しタスク自動生成はdiscretion=1で、ゴミ箱+undo基盤があれば安全化できるため**土台完成後に優先度を「中〜やや高」に引き上げてよい**と判断。一方サブタスクは複雑化リスクが反対理由であり、この軸(自動裁量)の対象外なので優先度は変わらず低いまま。双方向カレンダー同期は高頻度・高裁量でundoでも安全化しにくく、一方向iCalエクスポートに留める判断が補強された。
- **arch-agentの補正**: 「裁量の発生回数」は状態遷移図上の「未定義な遷移矢印の数」に翻訳できるが、正確には「未定義かつ頻繁に踏まれる矢印」でなければ実際の負担と一致しない(=頻度による重み付けが必要)。また、繰り返しタスクの優先度引き上げには「生成ロジックのバッチ化(1回の`save()`にまとめる)」が実装条件として必須という点でroadmap-agentと合意。
- **設計の衝突と解決(3者合意)**: arch-agentは「`load()`のたびに期限切れゴミ箱項目を自動物理削除する」を提案したが、test-agentは「自動パージは"気づいたら消えていた"という最悪の不意打ちになる」と反論。ux-agentが**「物理削除の実行」と「保持期限切れの判定」を分離**する折衷案を提示: `load()`/`list_items()`は読み取り専用のまま`deleted_at`から削除候補日数を計算するだけ(`is_overdue()`と同型)、実際の物理削除は`todo purge`という明示コマンドでのみ発生。arch-agent側も「元の提案は読み取り専用原則への違反だった」と認めて収束。
- **確定した具体的UI/CLI設計**: `todo rm`の成功メッセージに復元方法を明記、`todo restore <id>`新設、`todo list --status trash`でゴミ箱一覧、`todo purge [id] / --all-expired`で物理削除実行。Web UIは削除直後にトースト(8〜10秒)+ゴミ箱フィルタタブから恒常的にrestore可能。

## セッション1〜2を経て更新された"芯"(暫定)

1. 本体は体験、設計はその手段(変更なし)。
2. 信頼性は3層構成: ①クラッシュ耐性 ②バリデーション ③人間のミスからの復旧(undo/ゴミ箱、時間軸で2層に分離)。
3. 非対称設計の基準は「可逆性」ではなく**「新規の裁量(discretion)が発生する回数」×「頻度」**。可逆性はコストで買える副次的パラメータであり、同意の要否は最終的に「可視性が保証されない実質的不可逆」で判定する。
4. 統計・哲学的引用は仮説生成の道具(変更なし)。

次セッション以降の検討課題として、roadmap-agentから「discretion frequencyで説明できるリスク」と「discretion frequencyでは説明できないリスク(UXの複雑化・認知負荷)」という2種類のリスクの一般化、ux-agentから「`purge`が明示的すぎて誰も使わずゴミ箱が肥大化するリスク」が引き継がれている。

## セッション3: 発見された甘さと更新

- **roadmap-agentの一般化**: 機能追加のリスクは独立した2軸で構成されることが判明。
  - **軸A(裁量頻度リスク)**: undo・デフォルトルール・バッチ化で"買える"リスク(セッション1〜2の議論そのもの)。
  - **軸B(複雑性/認知負荷リスク)**: 新しい概念・データ構造・操作対象がユーザーのメンタルモデルに増えること自体のリスク。undoやprogressive disclosureでは一切解決しない。
  - 具体例で検証: 繰り返しタスク(軸A低・軸B低→実装推奨)、サブタスク(軸A非該当・**軸B高**→undo基盤があっても解決しない)、双方向カレンダー同期(軸A高・軸B高→却下)。
- **ux-agentの譲歩(重要な収束点)**: 当初「軸Bはprogressive disclosureで隠せる」という立場だったが、議論の結果**「軸Bが高い機能は、隠して作るのではなく、そもそも作らないという判断で対処すべき」**という、より厳しい結論に修正。理由: 実装の複雑性は「今は見えないが将来のUXリスクの先送り」であり、隠蔽がうまいほど"複雑な実装を良い設計だと錯覚させる"危険な副作用を持つため。UXは軸Bを「実装側の問題」として無視せず、複雑性が将来バグ・信頼毀損として跳ね返ってくる当事者だと認めた。
- **purge放置問題の解決(3者合意)**: 「ゴミ箱に一定数以上たまっても誰も気づかない」問題は、新機能ではなく既存パターンの拡張で解決。`todo list`実行時、パージ対象(保持期限"超過"のみ、総件数ではない)が1件以上あるときだけ末尾に一言("ゴミ箱に3件あります。整理するには: todo purge --all-expired")を添える。0件なら省略(5.8節の哲学を踏襲)。Web UIはバナーではなく「ゴミ箱(12)」という件数バッジのみ(CLIは一過性の画面なので能動的な注意喚起が要るが、Webは持続的に見える画面なのでバッジで足りるという使い分け)。この対策は軸A=0(データ不変)・軸B≈0(既存パターンの再利用)と判定され、「実装コストがほぼゼロならやっておいて損はない」という判断基準も同時に確立。
- **1手undoの詳細設計(arch-agent×test-agent、実装レベルまで確定)**:
  - `save()`は本体データを書く前に現在の`todo.json`を`todo.json.prev`へ退避(不変条件: 「save()が例外なく成功したら、直前状態は必ずprevとして復元可能」)。
  - `undo()`は`save()`を経由しない専用ルート: (1)prev読み込み(2)`load()`と共通の構造検証(3)mainへ直接反映(4)成功後にprevを削除。「prevを消費する」ことで2回目のundoは必ず`NothingToUndoError`となり、"redo化"を構造的に防止。
  - パージ境界日は既存の`is_overdue()`と同じ「境界日を含まない」規約に統一し、判定ロジックを単一ヘルパー(`_purge_eligible`)に集約。
  - **副次的発見**: `purge()`も`save()`経由のため、直前のpurgeも`todo undo`で1手だけ救える。「ゴミ箱(長期保護)」と「1手undo(直近保護)」が二重の安全網として機能する。
  - クラッシュ耐性を仕様化: prev確定後・本体確定前のクラッシュは実害なし。undoのmain反映後・prev削除前のクラッシュは次回undoが同一内容を再書き込みするだけのno-op(「1手だけ」の保証はクラッシュを跨いだ場合のみ緩やかに劣化するが実害なし、と割り切り)。
  - 9件のテストケース(パージ境界日3点+混在+冪等性+ロック経路+undo二重呼び出し+prev破損)で合意。

## セッション1〜3を経て更新された"芯"

1. 本体は体験、設計はその手段(変更なし)。
2. 信頼性は3層構成: ①クラッシュ耐性 ②バリデーション ③人間のミスからの復旧(undo/ゴミ箱の2層構造、実装設計まで確定)。
3. 非対称設計の基準は「新規の裁量(discretion)の発生回数×頻度」に加え、**機能追加そのもののリスクは「軸A(裁量頻度、undo等で解決可能)」と「軸B(複雑性・認知負荷、作らない以外に解決策がない)」の独立した2軸で判定する**。
4. 統計・哲学的引用は仮説生成の道具(変更なし)。

次セッションの検討課題: roadmap-agentから「軸Bの高さをどう定量的/半定量的に判定するか(現状は定性的判断に留まる)」という評価基準自体の精緻化、ux-agent/roadmap-agentから「注意喚起メッセージ(期限切れ・ゴミ箱肥大化等)が今後複数種類になった場合、個別実装せず単一の仕組みに統合すべきか」という将来課題。

## セッション4: 発見された甘さと更新

- **軸B(複雑性)の半定量スコアが確定(roadmap-agent×arch-agent)**: `score = (波及数(storage.py×2 + cli/server.py×1) + 新規不変条件数 + 新規語彙数) × (1 + 2×関係構造フラグ)`。関係構造(自己参照・木構造等)は加算でなく**乗算係数**にした点が重要(その機能単体のコストでなく、以降の別機能のコストまで増幅する性質のため)。検算結果: 繰り返しタスク=5、サブタスク=48、双方向カレンダー同期=さらに高スコア。既存の判断(推奨/非推奨/却下)と完全に整合。
- **「機能を増やすこと自体を目標にしない」原則の分解**: この1原則は実際には**軸A対応(既定非表示化で"隠して"採用してよい)**と**軸B対応(そもそも作らない)**という別々の原則が混ざっていたと判明し、分離して整理された。
- **advisory(注意喚起)統合設計が確定(3者合意)**: `storage.py`に`list_advisories(items) -> list[Advisory]`を置きCLI/Web共通で判定(`is_overdue()`と同型)。表示は常に最優先1件のみで、複数該当時の「ほのめかし文言」すら不要(該当が解消されたら次が自然に繰り上がるだけでよい、というux-agentの簡素化案を採用)。advisory(後回し可の状態通知)とエラー(即時認識すべき異常)は別カテゴリ。優先順位は「stale棚卸し > ゴミ箱肥大化」(棚卸し放置はリスト全体の信頼性を毀損するが、ゴミ箱肥大化は片付けの話に過ぎないため)。この設計自体の軸Bスコアは低いと判定(読み取り専用・新規状態やルールを追加しない・"advisory"はユーザー向け新概念ではない)。
- **DESIGN.md追記項目が確定8項目+検討中枠に仕分け(arch-agent×test-agent)**: 確定項目は、SQLite移行トリガーの2軸、ゴミ箱+1手undoの2層構造(統合しない理由込み)、境界仕様の統一(`is_overdue()`と同ルール)、undo専用ルート仕様、purge通知条件、purge/undoの`LockGuard`必須規約、ゴミ箱閲覧経路(`status="trashed"`)、undoの汎用性(remove専用ではない)。軸A/B判断フレームワークはまだ「検討中」枠。
- **実装コスト見積り(test-agent)**: 既存163テストへの影響は軽微(10件程度の修正)。新規テストは約55本(storage層31本+CLI12本+server12本)。arch-agentから「Phase 1: storage.py+CLI(約40本)を先に固め、Phase 2: server.py配線(約12〜15本)を後追いする」段階実装が提案され、test-agentもテスト書き直しコストの観点から賛成。ただし「Web公開を遅らせてよいか」はプロダクト判断であり、技術側だけでは決定できないとして roadmap-agent/team-lead に委ねられた。

## セッション1〜4を経て更新された"芯"

1. 本体は体験、設計はその手段(変更なし)。
2. 信頼性は3層構成: ①クラッシュ耐性 ②バリデーション ③人間のミスからの復旧(undo/ゴミ箱の2層構造、実装設計・テスト設計まで確定)。
3. 機能追加のリスクは独立した2軸で判定する: **軸A(裁量頻度、undo等の実装コストで解決可能)**と**軸B(複雑性・認知負荷、半定量スコアで測定し、閾値超なら"作らない"以外の解決策がない)**。「機能を増やすことを目標にしない」という当初の1原則は、この2軸に分解されることで初めて実務判断に使えるようになった。
4. 統計・哲学的引用は仮説生成の道具(変更なし)。
5. **(新設)議論の成果は仕分けて記録する**: 複数セッションの相互検証を経て収束した項目は「確定」、まだ検証途上の項目は「検討中」と明示して区別する。技術的に可能かどうかとプロダクトとして採用すべきかどうかは別の意思決定であり、後者は該当領域の担当(ロードマップ)に委ねる。

次セッションの検討課題: roadmap-agentから「軸A・軸Bのスコアを新規機能提案の"入口審査"としてどう運用するか(誰が採点するか、閾値、閾値超過時の扱い)」という運用ルール化。

## セッション5: 発見された甘さと更新

- **入口審査の運用ルールが確定(roadmap-agent×arch-agent×test-agent)**: 採点者=提案者の自己申告→arch-agent(軸Bの妥当性検証)→test-agent(軸Aのミティゲーション可能性判定、5項目チェックリスト: ①既存の緩和パターンで足りるか②失敗が静かに起きないか③判断タイミングを分離できるか④元に戻すコストが壊すコスト以下か⑤既存テスト手法の延長で検証できるか)→roadmap-agent(需要と両軸スコアを統合し最終判断)。閾値(暫定): 軸B<10は通常進行、10〜25は複雑性封じ込め策を明記の上進行、25超はデフォルト却下。**重要な限定**: この審査プロセス自体は「新しいデータ構造・関係性を持ち込む提案」にだけ適用し、既存パターンの延長には適用しない(審査の儀式化自体が新たな軸Bコストになるため、という自己言及的な歯止め)。
- **Phase分割(CLI先行・Web遅延)に賛成で決着(roadmap-agentの最終判断)**: 軸Bスコアが目安5を超える非自明な新機能は、CLI実装→実運用検証→データモデル安定後にWeb反映、という順序を既定にする。軸Bが低い機能(advisory等)は同時実装のままで良い。
- **軸Bの判定基準を再定義(arch-agent×roadmap-agent)**: 「ファイルが増えたか(波及数)」ではなく「共有不変条件(shared invariants)に新しい状態を持ち込むか」が本質。`advisories.py`のような読み取り専用の派生モジュールは、ファイルが増えても軸Bスコアはゼロ(`todo.json`を書き換えないため)。ファイル分割=実装整理(軸Bと無関係)、新しい永続状態の導入=軸Bの評価対象、という基準で明確化。
- **storage.py分割案がv2に更新(arch-agent)**: 新機能追加を踏まえ、`todo/storage/`パッケージ化(`storage.py`はfacadeとして互換維持)。`errors.py`/`atomic_io.py`(新設: 低レベルのアトミック書き込みプリミティブのみ)/`lock.py`/`validation.py`/`repository.py`(状態遷移を伴う書き込み系)/`queries.py`(既存の一覧・検索)/`advisories.py`(新設: 横断的な要約計算)。
- **最高リスク箇所の特定と対策(test-agent×arch-agent)**: 新機能追加で最もリスクが高いのは新規関数ではなく`save()`自体への変更(全操作が経由する唯一の関数のため、既存163テストが依拠する保証全体に触れる)。対策: 既存`save()`の中身を**ロジックを一切変えずに**`atomic_io.py`の`_atomic_write()`として抜き出す「リファクタリング」と位置づけ、新しい`save()`は「prev退避(新規)→`_atomic_write()`(既存ロジックそのまま)」という薄いオーケストレーションにする。実装順序も確定: ①モジュール分割のみをコミットし既存163件が無修正で通ることを確認 → ②新機能(約55本)を追加するコミット群。これにより「リファクタのミスか新機能のバグか」を即座に切り分けられる。
- **UXの発見可能性を検証(ux-agent×arch-agent)**: `restore`(削除の取り消し)は「気づかなくても機能的には困らない、気づけば得をする」安全網であり、無理に気づかせる必要はないと判断。一方、`--help`からの受動的発見経路にargparseの実装上の盲点を発見: `--status`引数に`choices=[...]`を明示しないと新しい値(`trash`)が`--help`に表示されない、という修正項目が追加された。
- **未解決のまま「検討中」に留めた懸念(ux-agent提起、team-lead判断)**: 「誰もpurgeを実行せずゴミ箱が無限蓄積し、"件数が少ない"というJSON全件読み書きの前提が崩れる」リスクは、現状のスコープ(個人用・小規模)では過剰投資と判断し、新たな設計はせずDESIGN.mdの「検討中/将来課題」に一言記載するに留める(SQLite移行トリガーと同種の「閾値だけ決めておき今は動かさない」扱い)。

## セッション1〜5を経て更新された"芯"

1. 本体は体験、設計はその手段(変更なし)。
2. 信頼性は3層構成(クラッシュ耐性/バリデーション/undo・ゴミ箱)に加え、**「新機能追加が既存の信頼性保証を壊すリスク」への警戒(リファクタリングと機能追加のコミット分離)**が4本目の柱として追加された。
3. 機能追加のリスクは軸A(裁量頻度、5項目チェックリストで判定)×軸B(複雑性、半定量スコア+入口審査プロセスで判定)の2軸。審査プロセス自体にも軸Bの発想を自己適用し、儀式化を防ぐ歯止めを持つ。
4. 統計・哲学的引用は仮説生成の道具(変更なし)。
5. 議論の成果は「確定」と「検討中」に仕分けて記録する(変更なし)。技術的な実現可能性とプロダクトとしての採用可否は別の意思決定であり、スコープを超える懸念は「今は対応しない」という判断も正当な結論として記録する。

次セッションの検討課題: roadmap-agentから「これまで確定した一連のフレームワーク(3分類・裁量頻度×頻度・軸A/軸B・入口審査・Phase分割)を実際にDESIGN.mdへどう反映するか、文書構成の最終確認」。

## セッション6: DESIGN.md文書構成の確定

4者(ux-agent/arch-agent/test-agent/roadmap-agent)の討議で、これまでの全成果をDESIGN.mdへ反映する章立てが衝突なく確定した。

- **2章(コマンド一覧)**: `restore`/`purge`/`--status trash`の使用例を追記。
- **3章(データモデル)**: `deleted_at`フィールドを追記。
- **4章(ストレージ形式と保存場所)**: SQLite移行トリガー(2軸)、1手undoの仕組み(`todo.json.prev`)、undo専用ルート仕様(redo防止)、`LockGuard`必須の実装規約。
- **5章(UX上の設計判断とその理由)**: 削除は取り消し可能にする(rmはゴミ箱行き、成功メッセージに復元方法を明記)/物理削除は明示コマンドのみ/advisoryの表示ルール(最優先1件のみ、ほのめかしなし)。
- **6章(議論の記録)の延長として新小節「6.5 機能採否の判断基準(検討中)」**: 3分類・裁量頻度×頻度・軸A/軸B・入口審査・Phase分割をここに集約。7章「スコープ外」の直前に置き「判断基準→適用結果」という自然な読み順にする。
- **9章(テスト計画)**: 個別のテスト関数名ではなく、既存の粒度(仕様・境界条件・不変条件を文章で明記する粒度)に合わせてゴミ箱/undo/アトミック書き込み分離のテストを追記。
- **10章(実装の段取り)**: storage.pyパッケージ化(`atomic_io.py`/`advisories.py`新設)、リファクタと新機能追加のコミット分離方針。

**重要な自己修正(roadmap-agent)**: 当初roadmap-agent自身が提案した「独立した新章(9章 機能拡張の判断フレームワーク)」案を、既存9章が既にテスト計画で使用済みだったことに加え、**「新しい最上位章を作ること自体が、この討議全体で警戒してきた軸B的な複雑性の追加(文書構造への新概念の導入)に該当する」**と自ら気づいて撤回。arch-agent提案の「既存6章の延長としての6.5節」を採用した。ドキュメント構成の判断自体にも、確立した軸A/B原則(複雑性を増やさない)が一貫して適用された。

## セッション1〜6を経て更新された"芯"

1〜5は変更なし。

6. **(新設)原則は自分自身にも適用する**: 「複雑性を増やさない」という原則は、機能追加だけでなく、審査プロセス自体(セッション5)やドキュメント構成自体(セッション6)にも自己適用されるべきものであり、実際にそう運用されて自己矛盾が起きないことが確認された。

DESIGN.mdへの反映作業自体はarch-agent中心に進める前提が整った。次セッションでは、roadmap-agentから提起された「繰り返しタスク・iCalエクスポートの具体的な次アクション」、および「この討議全体をどう終息させるか」を検討する。
