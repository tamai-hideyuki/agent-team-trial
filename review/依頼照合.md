# 依頼照合レビュー(req-check)

対象: `DESIGN.md`

依頼の「DESIGN.md に足すこと」7項目 + 完了の定義を、DESIGN.md の実際の記述と1項目ずつ照合する。

## 「足すこと」7項目

| # | 項目 | 判定 | 根拠(行番号) | 備考 |
|---|---|---|---|---|
| 1 | 新しいコマンドとオプションの使用例 | **はい** | 2章 表: 29-33行(新オプション・新規サブコマンド一覧)、使用例: 66-106行(`todo add --due --priority --tag`, `edit`, `undone`, `list --sort/--search/--tag`、異常系の使用例含む) | 正常系・異常系(95-106行)双方あり |
| 2 | データモデルの変更と、既存データの扱い | **はい** | 3章: 108-135行。`due`/`priority`/`tags` の追加(108-122行)、旧データでキー欠損時の扱い(129行, 131行)、ラウンドトリップ安全性の方針(133-135行) | |
| 3 | ブラウザ画面の画面・操作と、サーバのAPI | **はい** | 8.1節 251-258行(画面構成: フィルタバー/追加フォーム/一覧テーブル/エラー表示欄)、8.2節 260-270行(API仕様表とエラー応答形式) | |
| 4 | CLI と画面を同時に使ったときの扱い(ロック) | **はい** | 4章「CLIとブラウザ画面での共有」163-167行(同一ロックファイル・`threading.Lock`によるプロセス内直列化・ロック保持区間の最小化)、8.3節の「同時実行」278行(4章参照の形で再掲) | |
| 5 | ローカルサーバの安全性(外から繋がらないこと・ブラウザの他のサイトから操作されない対策) | **はい** | 8.3節 272-278行。バインドアドレス127.0.0.1限定(274行)、DNS rebinding対策としてHostヘッダ検証(275行)、CORSヘッダ非付与(276行)、CSRFトークンをカスタムヘッダ`X-Todo-Token`で送る方式(277行) | |
| 6 | テストの計画 | **はい** | 9章 280-286行。単体テスト・CLI結合テスト・互換性テスト・サーバ結合テスト・同時実行テストの5種を明記 | |
| 7 | 実装の段取り: どのファイルを誰が持つか、タスクと依存関係 | **部分的** | 10章 288-300行。タスク分割(実装-1〜5)・担当(実装担当A/B/C)・依存関係(292-298行の表)は明記されているが、「どのファイルを誰が持つか」に対応する具体的なファイル名(例: storage.py, cli.py, server.py相当)は記載がない。担当列はチャプター単位(3章・4章、2章・5章、8.2章、8.1章、9章)の役割分担であり、ファイル単位の分担ではない | 依頼文言は「どのファイルを誰が持つか」と明示しているため、チャプター単位の分担では厳密には未充足 |

## 完了の定義

> DESIGN.md が更新され、3人の合意と、採用しなかった案とその理由が、DESIGN.md の6章に残されていること

| 要素 | 判定 | 根拠(行番号) |
|---|---|---|
| DESIGN.md が更新されている | **はい** | 全体(1-301行)に期限・優先度・edit/undone・検索・タグ・ブラウザ画面の追加記述が確認できる |
| 3人(design-agent/ux-agent/critic-agent)の合意が6章に残っている | **はい** | 186-229行。「設計担当(design-agent)の初期案に対し、ux-agent(UX観点)とcritic-agent(反論役)からレビューを受け、以下のように対応した」(188行)という前提のもと、指摘元・指摘内容・対応の表が2つ(190-229行)ある | 「合意した」という明示的な一文はないが、指摘への対応(採用)が記録されており、実質的な合意記録として妥当と判断 |
| 採用しなかった案とその理由が6章に残っている | **はい** | 231-235行「却下(採用しなかった)提案とその理由」に2件(ブラウザ画面をGET専用に縮小する案、閲覧・検索・トグルのみに限定する段階案)+基本CLI部分は「なし」との記載あり | |

## 総評

7項目中6項目は明確に充足。項目7(実装の段取り)のみ、依頼文の「どのファイルを誰が持つか」の"ファイル"という粒度に対し、DESIGN.md 10章は"チャプター(機能)"単位の分担にとどまっており、部分的な充足と判断した。他は完了の定義も含め問題なし。

## 更新履歴
- 初回レビュー: 7項目中6項目「はい」、項目7のみ「部分的」(ファイル分担の欠如)を指摘し ops-coverage/testability/impl-ready に共有。
- 一時的に「10章がファイル単位に更新された」との情報を受け項目7を「はい」に変更したが、これは停止済みのdesignエージェントが誤って再起動し、レビュー対象のDESIGN.mdを規約違反で編集した内容に基づく誤判定だった。team-leadが10章を原状回復したため、項目7の判定を「部分的」に差し戻した(2026-09-12)。
